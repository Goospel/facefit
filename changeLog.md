# changeLog — facefit

> 완료 기록(역순). 코드 세부는 PR·커밋에 있으니 여기는 **왜 / 무엇을**만 적는다.
> 앞으로 할 일은 [`plan.md`](plan.md), 함정은 [`claude-docs/troubleshooting.md`](claude-docs/troubleshooting.md).
> PR 번호는 적지 않는다 — 미래에 확정되는 값이라 stale이 조용히 쌓인다. 찾으려면 제목으로 `git log --grep`.

## 2026-08-29 · 화장품 제품 데이터 API 리서치 — v2 계단에 반영

한국 출시 화장품의 제품 단위 공개 API가 있는지 조사해
[문서로 남겼다](docs/2026-08-29-cosmetics-data-api-research.md). 전 제품 API는 구조적으로
없고(품목 등록제가 아님), **1순위는 식약처 기능성화장품 보고품목 API**(무료·20만 건 —
v2 2단계 프록시에 얹어 제품명 자동완성), 성분 사전은 식약처 원료성분정보(1단계 번들 내장).
코리안넷 표준DB는 바코드 건당 유료 협의제라 후순위 — 공공포털 무료 바코드 API들이
2018년부터 동결된 것도 대한상의의 유료 트랙 회수 때문임을 확인했다. 구상 §5-7과
plan ⏸절에 링크를 걸었다. 코드 변경 0.

## 2026-08-29 · 콘솔 앱 생성 + 첫 번들 테스트 푸시 (태스크 10 전반)

콘솔에 미니앱을 등록했다 — miniAppId **70438**, `appName: facefit`(중복 없이 확보,
`intoss://facefit`), 카테고리 생활>일상>뷰티, 앱정보 검토는 제출 즉시 APPROVED.

아이콘은 restfit 파이프라인 그대로 — `icon.svg` 단일 출처(파인더 프레임 + 어제의 고스트
윤곽 + 뺨 반짝임, 토스 블루 계열) → `render-icon.ps1` Chrome 래스터화 → 600px 업로드.
업로드 검증은 HTTP 200 + 전송 바이트 = 파일 크기.

**개인정보처리방침은 콘솔에 별도 등록 자리가 없다**(설계 §6의 가정과 달랐다).
MCP의 `miniapp_update_privacy_policy`는 토스로그인 약관 전용이고, restfit도 별도 방침
문서 없이 검수를 통과했다 — 로그인·수집 0인 앱은 「기기 전용」 고지를 앱 안(온보딩)과
상세 설명에 싣는 것으로 충분하다. 상세 설명 마지막 줄에 「휴대폰에만 저장, 서버 전송
없음」을 박았다.

테스트 262건 그린 확인 후 `npm run release`로 번들 `20260829-1`(62,757바이트)을
업로드·컴파일(CREATED)하고 테스트 푸시까지 발송했다. `ait build`가 deploymentId를
stdout에 그대로 찍어 줘서 번들 헤더를 열 필요가 없었다.

남은 것: 실기기 확인(`FLASH_MS`·`BASE_FPS` 체감) → 검수 제출 → 콘솔 웹 「출시하기」.

## 2026-08-29 · v1 구현 (태스크 1~9)

설계 문서대로 미니앱을 세웠다 — 촬영(고스트 + 셔터 플래시 + 저장 직후 관찰 1문항) ·
제품 CRUD · 월간 캘린더 · 타임랩스(제품 구간 바) · 온보딩 · 3탭 배선.

restfit에서 검증된 것(카메라 수명·캡처 파이프라인·달력 산수·사진 저장소·확대 금지 가드)은
그대로 이식하고, facefit 고유의 판단만 새로 짰다. 서버는 0대 — 사진은 IndexedDB,
제품·관찰은 localStorage, 런타임 네트워크 호출 없음.

남은 것은 태스크 10(콘솔 앱 생성·개인정보처리방침·실기기 확인)뿐이다.
`FLASH_MS`(500) · `BASE_FPS`(6)는 거기서 실기기 체감으로 확정한다.

## 2026-08-29 · v1 설계 문서

restfit 이식 범위·스키마·태스크 분해를 한 장으로 확정했다. 결정 7건(관찰 기록 탭 포함 ·
셔터 플래시 · 고스트 기준 = 기준 사진 · 3탭 · 타임랩스 조작 · 비교 화면 제외 · 레포 구조).

## 2026-08-29 · 서비스 구상

화장품이 효과가 있는지 사진으로 관찰한다 — restfit의 눈바디·고스트 시스템을 얼굴로 옮기는 구상 확정.
