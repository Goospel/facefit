# changeLog — facefit

> 완료 기록(역순). 코드 세부는 PR·커밋에 있으니 여기는 **왜 / 무엇을**만 적는다.
> 앞으로 할 일은 [`plan.md`](plan.md), 함정은 [`claude-docs/troubleshooting.md`](claude-docs/troubleshooting.md).
> PR 번호는 적지 않는다 — 미래에 확정되는 값이라 stale이 조용히 쌓인다. 찾으려면 제목으로 `git log --grep`.

## 2026-09-03 · 기름종이 알림 앱 조각 (C-1~C-4)

v5 설계의 클라이언트 절반. 체크 1회 = 알림 1회라는 **승인 게이트**를 화면에 세웠다 —
오늘 탭 「아침 알림」 행 바로 아래 카드에서 「썼어요」를 누르면 동의를 확인하고 서버에 예약하고,
알림이 간 뒤에는 「썼어요 · 다음 알림」으로 한 번 더 승인해야 다음이 있다. 날짜가 바뀌면 저절로
처음으로 돌아간다(KST 기준) — 야간 상한을 안 두기로 한 근거가 이 하루짜리 게이트다.

상태의 단일 출처는 기기의 `facefit.oilNextAt` 하나이고 **화면은 서버를 조회하지 않는다.**
서버가 실패하면 저장하지 않는다 — 예약이 안 됐는데 「예약됨」으로 그리면 오지 않을 알림을
기다리게 된다. 「그만 받기」는 반대로 로컬을 먼저 지우고 서버 결과를 안 기다린다(무음 폴백).
카드는 **어느 상태에서도 채운 파란 버튼을 안 쓴다** — 오늘 탭의 파란 자리는 「오늘 얼굴 찍기」
하나뿐이라, 하루에 여러 번 반복되는 부수 행동이 그 신호를 나눠 가지면 「지금 할 일」이 흐려진다.
푸시를 탭하면 `Environment.initialURL`을 읽어 오늘 탭에서 시작한다 — 승인 버튼에 한 번에 닿는 유일한 장치다.

⚠️ **아래 서버 조각이 배포돼야 실제로 예약된다.** 그 전까지 「썼어요」는
「지금은 예약할 수 없어요」로 끝난다(무음 폴백이라 다른 기능은 그대로다).

## 2026-09-03 · 기름종이 알림 서버 조각 — 예약 한 줄 · 분마다 도는 워커 (S-1~S-4)

「썼어요」를 누르면 3시간 뒤 알림 1건. 앱 안 타이머는 웹뷰가 닫히면 안 돌고 콘솔 정기 발송은 개인별
시각이 안 되니, 남는 길은 **서버가 직접 보내는 것** 하나였다(설계 §3-1). `PUT/DELETE /v1/reminder` 둘과
`reminder` 테이블, 분마다 도는 워커를 놓았다. 시각은 **서버가 정한다** — 「3시간」이 프로퍼티 한 곳에만
살아 바꿀 때 클라를 안 건드린다. 야간 상한은 두지 않았다(사용자 결정) — 밤을 막는 장치는 「다음 알림
승인」 게이트 하나이고, 그 결정에 회귀 가드를 붙였다.

익명 키는 곧 백업 열람 토큰이라 평문으로 눕힐 수 없어 **AES-GCM으로 봉인해** 둔다(행 수명 약 3시간).
워커는 **보내기 전에 시도를 기록**한다 — 뒤에 기록하면 크래시 창에서 같은 행이 매분 다시 나가 알림
폭탄이 된다. 전부 **다크런치**다: KEK·템플릿 코드·mTLS 인증서 중 하나라도 없으면 서버는 그대로 뜨고
알림만 쉰다(PUT은 503, 워커는 1회 경고 후 스킵). 그래서 콘솔 절차가 끝나기 전에 머지해도 안전하다.

## 2026-09-03 · 콘솔 상세 설명을 검수 중인 번들에 맞췄다 — 백업 문장 · 촬영 경로 · 사용 빈도

`20260902-16` 검수가 이전 7건(5~19분)과 달리 12시간 넘게 「대기」라, 번들과 앱 정보의 간극을 찾았다.
릴리스 노트는 「백업에 사용 기록도 함께 저장」인데 상세 설명은 「제품과 관찰 기록만」 — 서버로 가는
데이터 범위가 늘었다고 릴리스 노트가 먼저 말해 **개인정보 문장이 불일치**했다(사용자 가설: 간극은 반려가
아니라 지연으로 나타난다). 상세 설명을 고쳤다 — 백업 문장을 「제품·관찰·사용 기록만」으로, 촬영 경로를
「'제품' 탭 맨 위나 '오늘' 탭」으로(`-18`의 2차까지 참), 3번에 「가끔」 제품 한 문장. 500자 제한에 맞추느라
다른 문장을 조금씩 줄였다(495자).

덤으로 T-014의 판정 기준이 바뀌었다 — `miniapp_update_basic_info`는 **검토 대기 초안**을 만들고
`miniapp_get`은 승인 전까지 라이브 값을 보여준다. 접수 여부는 `miniapp_meta_status.reviewState`로 본다
(`IN_REVIEW` = 접수 · `APPROVED` 그대로 = no-op). 지난번은 후자였고 이번은 전자였다.

## 2026-09-03 · UX 1차+2차 테스트 번들 (`20260903-18`) 실기기 확인

2차 머지 직후 main `a850859`로 번들을 올리고(261.99kB · sdk 3.1.1) 테스트 푸시 → 사용자가 실기기에서
첫 진입 카드(①만 파랑 · 폼을 열어도 고지 잔류) · 찍기 CTA → 찍은 뒤 파란 버튼 0개 · 기록 탭 변화 보기
카드와 파란 「재생」 · 삭제 격리를 확인했다. 어제의 `-17`(1차만)은 이 번들이 대체해 제출하지 않는다.
**검수 제출은 아직이다** — `20260902-16`이 하루 넘게 검수 중이라 콘솔이 제출을 막는다.

## 2026-09-02 · UX 개선 2차 — 파란 버튼이 지금 할 일을 따라간다

**왜**: 처음 온 사람이 「매일 한 장」을 배운 직후 첫 화면(제품 탭)에 닿는데, 그 화면은 **등록만** 권하고
찍기는 정보 행처럼 서 있어서 둘의 관계를 두 번 읽어야 했다. 반대로 이미 찍은 날에도 같은 자리가
계속 「찍기」를 걸고 있어, 끝난 일을 재촉하는 것처럼 보였다. 기록 탭에서는 이 앱의 보상(얼굴이
어떻게 달라졌나)이 달력 밑 회색 「재생」에 묻힌 채 빨간 「사진 모두 삭제」와 붙어 있었다.

**무엇을**: 1차의 「파란 버튼은 하나」를 화면 **구조**에까지 밀어, 파란 자리가 상태를 따라 옮겨 다니게
했다 — 제품 0개면 첫 진입 카드의 「제품 추가」, 제품이 있고 안 찍었으면 「오늘 얼굴 찍기」, 찍은 날엔
파란 버튼이 **없다**(정정용 「다시 찍기」만 회색). 첫 진입 카드는 할 일 둘을 번호로 말하고, 그 ② 자리는
찍기 CTA·찍은 뒤 행을 **그대로 재사용**해 제품 없이 사진부터 찍은 사람에게도 화면이 거짓말을 안 한다.
기록 탭은 변화 보기 카드를 달력 위로 올려 「재생」을 그 화면의 유일한 파란 버튼으로 두고, 아래 줄을
「최근 M/D」에서 **기간**으로 바꿨으며(타임랩스가 보여 주는 것이 기간이다), 「사진 모두 삭제」는 구분선
아래로 격리했다. 접근성 이름은 전부 그대로다. 서버·저장소 0 변경.

## 2026-09-02 · UX 1차 테스트 번들 (`20260902-17`) 실기기 확인

UX 1차 머지 직후 main `7f061f6`으로 번들을 올리고(260.21kB · sdk 3.1.1) 테스트 푸시 → 사용자가 실기기에서
스위치 꺼짐/켜짐 그림 · 켰다 끈 뒤 테두리 복귀 · 알림 알약 · 촬영 한 줄을 확인했다. **검수 제출은 아직이다** —
앞선 `20260902-16`이 검수 중이라 콘솔이 제출을 막는다. 그 검수가 끝나면 -17을 제출한다(반려면 수정을 얹어
다시). 이 기록은 jsdom 판정뿐이던 PR의 「스크린샷 없음」을 실기기 확인으로 닫는 항목이다.

## 2026-09-02 · UX 개선 1차 — 스위치 켜짐·꺼짐 대비, 촬영 셔터 한 개

**왜**: 두 군데서 「지금 켜진 건지 꺼진 건지 모르겠다」가 반복됐다. 백업 스위치는 상태를 파랑↔회색
**색 하나**로만 갈랐고, 촬영 화면은 「촬영」·「3초 후 촬영」이 나란히 서서 **파란 버튼이 둘**이라
어느 쪽이 셔터인지 매번 읽어야 했다. 알림 행은 백업 스위치와 같은 카드 가족이라 행동인데도
스위치의 상태 표시처럼 읽혔다.

**무엇을**: 원칙 둘을 실행했다. ① **켜짐과 꺼짐은 색·표식·글자 셋으로 가른다** — 스위치에 체크
표식과 아래 「켜짐/꺼짐」 글자를 더하고, 아래 줄 문장도 「켜져 있어요/꺼져 있어요」로 **시작**하게
했다(셋 중 어느 하나만 봐도 상태를 안다). 켜진 행은 카드 톤까지 바꾼다. ② **화면에 파란 버튼은
하나** — 촬영 화면을 `[3초 칩] (셔터) [겹치기 칩]` 한 줄로 바꿔 셔터를 유일한 큰 버튼으로 세우고,
「3초 뒤에 찍을까」와 「겹쳐 볼까」는 셔터가 아니라 **셔터의 설정**이라 스위치 칩으로 내렸다.
알림 행 오른쪽은 알약(누르면 열린다는 신호)으로, 단 켜진 뒤에는 알약을 걷고 체크 + 「켜짐」으로.

첫 진입 안내 카드·제품 탭 찍기 CTA·기록 탭 타임랩스 승격은 화면 **구조**가 바뀌는 축이라 이번
범위 밖이다 — 설계 문서를 따로 올린다.

## 2026-09-02 · v4-2 계단 1 검수 제출 (`20260902-16`)

클라 PR 머지 직후 번들을 올리고(257.88kB — 키가 박힌 production 값) 테스트 푸시 → 사용자가 실기기에서
설계 §5 절차를 확인(가끔 제품 폼 안내 줄 · 촬영 칩 블록 · 카드 「1회」 · 타임랩스 배지 · 다시 찍기 미리 채움 ·
**매일 제품만이면 화면 0 변경** · 백업 PUT) → 검수 제출(`REVIEWING`). 릴리스 노트는 「매일 쓰는 제품만
등록한 경우 화면은 그대로예요」로 끝나 검수자가 옛 동선의 무변경을 기대하게 했다.

남는 것 하나 — 콘솔 상세 설명의 백업 문장 「제품과 관찰 기록만」이 사용 기록이 실리는 지금은 좁다.
앱정보 MCP 수정은 못 믿으므로(T-014) 사용자가 콘솔 웹에서 고친다.

## 2026-09-02 · v4-2 클라 — 사진에 「이 전에 쓴 것」을 단다 (계단 1)

실기기 피드백에서 출발했다 — 「로션·클렌징폼은 매일 쓰는데 팩은 일주일에 한두 번인데, 지금
서비스는 다 매일 쓴다고 가정한다」. 빈도 필드만 두는 안(A) 대신 **실사용 로그**(B)를 택했다:
안 쓴 날의 사진이 쓴 날 사진의 **대조군**이 되어 그 사진에도 의미가 생기고, 빈도 하나로 뭉갠
기록은 그 대조를 못 만든다.

- 제품에 **사용 빈도**(매일 / 가끔)가 생겼다. 이 필드가 하는 일은 하나 — **촬영 때 물어볼
  제품인가**. 매일 제품은 구간 안에 대조군이 없고, 매일 n개 체크는 촬영 루프를 죽인다.
- 촬영 뒤 **관찰 1문항과 같은 화면**에 「어제부터 지금까지 쓴 게 있나요?」 칩이 선다. 관찰 답과
  건너뛰기가 이미 나가는 버튼이라 제출 버튼이 안 늘고, **안 쓴 날의 탭 수는 0 증가**다.
  가끔 제품이 없는 사용자의 화면은 한 글자도 안 바뀐다.
- 로그는 **사진의 속성**이다(키 = 사진 날짜 · 뜻 = 그 사진 직전 루틴). 「팩은 밤에 하고 사진은
  아침에 찍는다」는 시점 불일치가 정의로 해소되고, 사진·관찰·로그가 같은 키라 대조가 문자열
  비교 하나로 끝난다.
- **3상을 배열의 유무로 표현한다**: 키 없음(안 물어봄) · `[]`(물어봤고 안 썼다) · `[id…]`(썼다).
  건너뛰기로 나간 날도 「안 썼다」로 남는다 — 안 남기면 그 사진이 대조군이 못 된다.
- 카드는 가끔 제품에 한해 「n일째」 대신 **「n회」**(기록된 사진 날짜 수)를, 타임랩스 프레임
  배지는 「8월 3일 · 팩」 / 「· 쓴 것 없음」 / 날짜만을 적는다.
- 백업 블롭에 최상위 키 `usage`가 하나 늘었다 — **서버 화이트리스트 변경이 짝**이라 서버를
  먼저 배포한 뒤 이 클라가 나간다. `schemaVersion`은 1 유지(선택 키라 올릴 이유가 없다).

계단 2(타임랩스 바의 사용일 점 · 기록 캘린더 시트에서 보기·고치기)는 실기기로 일주일쯤 써 본
뒤 연다 — 배지만으로 대조가 읽히면 점은 안 찍는다.

## 2026-09-02 · 백업 블롭에 사용 기록 키를 연다 (v4-2 계단 1 · 서버)

「팩은 일주일에 한두 번인데 앱은 전부 매일 쓴다고 가정한다」는 실기기 피드백에서 나온 v4-2의
첫 조각. 사진마다 「그 전에 뭘 썼나」를 남기려면 백업 블롭에 최상위 키 `usage`가 하나 는다 —
서버 화이트리스트가 넷이라 **그대로면 새 클라의 백업 PUT이 전부 400**이다. 그래서 서버가 먼저 간다.

- `BlobValidator`: `usage` 화이트리스트 · `MAX_USAGE_DAYS = 4_000`(`MAX_NOTES`와 같은 논리 — 날짜 1키라
  10년치가 넘는다) · 객체인지와 키 수만 본다.
- **`usage` 안은 안 본다.** 키 없음 / `[]`(물어봤고 안 썼다) / `[id…]`의 3상은 클라의 뜻이라,
  서버가 해석하면 클라가 뜻을 넓힐 때마다 서버가 따라 바뀐다 — 제품 항목을 불투명하게 두는 것과 같은 규율.
- 옛 클라 블롭(키 넷)은 그대로 200. 컨트롤러·리포지토리·마이그레이션 0 변경(블롭은 불투명 문자열).
- ⚠️ **배포 순서**: 이 서버가 라이브가 된 뒤에 클라 PR을 머지한다.

## 2026-09-02 · v4-1 후속 라이브 출시 (`20260902-15`)

후속 PR 머지 직후 번들을 올리고, 사용자가 실기기에서 **iOS 피커가 오늘 이후·시작일 이전을
막는 것**(jsdom이 못 재던 유일한 항목)을 확인한 뒤 검수를 요청했다 → 승인 → 사용자가 콘솔 웹
「출시하기」 → **라이브**(15:05 · `deployed: true` 실측). **검수 무반려 7연속.**

오늘 하루에 라이브가 셋이다 — iOS 날짜 칸 수정은 `-14`가 흡수했고, 첫 화면 변경(`-14`)과
그 후속(`-15`)이 각각 출시됐다. 제출 기록과 출시 기록을 이번엔 한 항목으로 묶었다 — 후속 계단이
작아 둘로 나눌 이유가 없었다.

## 2026-09-02 · v4-1 후속 — 관찰 문구 사본 통합 · 미래 날짜를 입력에서 막는다

리뷰가 남긴 후속 둘을 같은 날 닫았다.

- `FacePhoto`의 선택지 배열이 관찰 문구의 **세 번째 사본**이었다 — `storage`의 어휘·문구 한 벌에서
  `VERDICTS.map` 한 줄로 만든다. 이제 문구를 바꾸면 촬영 화면 테스트가 같이 죽는다(사본이던
  때는 안 죽었다).
- 시작일·종료일에 미래를 넣으면 카드에 「-6일째」나 아직 안 온 날수가 섰다(main에서도 `D+-7`).
  기록은 「오늘까지 쓴 것」이라 미래는 값이 아니다 — 피커 `min`/`max`로 막고 데스크톱 타이핑은
  저장 조건이 막는다. 빈 시작일도 함께 막는다(저장소가 다시 읽을 때 조용히 버리던 구멍).
  **표시만 자르는 안은 기각** — 증상만 가리고 「종료」 섹션에 미래 시작 제품이 서는 문제가 남는다.

## 2026-09-02 · v4-1 라이브 출시 (`20260902-14`)

검수 승인 → 사용자가 콘솔 웹 「출시하기」 → **라이브**(13:55 · `deployed: true` 실측).
**검수 무반려 6연속.** 앱을 열면 이제 얼굴이 아니라 제품 목록이 먼저다.

이번 계단에서 남는 것 둘 — ① 「오늘」 탭이 덜 열린다. 촬영 답은 카메라 화면에서 받으므로
기록엔 영향이 없지만, 그 탭에 남은 「쓰는 중」 칩·알림 행이 비어 가면 다음 계단에서 정리한다
(설계 §6). ② 후속 후보는 plan.md v4-1 절 끝의 메모(관찰 문구 세 번째 사본 · 미래 날짜 판정).

## 2026-09-02 · v4-1 검수 제출 (`20260902-14`)

머지 직후 번들을 올리고 사용자가 실기기에서 확인한 뒤 검수를 요청했다(`REVIEWING`).
아침의 iOS 날짜 칸 수정(`20260902-13`)은 따로 제출하지 않고 이 번들이 흡수한다.

릴리스 노트는 검수자 안내다 — 첫 화면이 제품 탭이라는 것, 맨 위 상태 줄이 사진 없이 촬영으로
가는 것, 카드를 누르면 폼이 열리고 종료·삭제가 그 안에 있는 것, 「추가」가 제목 오른쪽인 것을
적었다. 콘솔 상세 설명은 「'오늘' 탭에서 촬영해요」로 시작하는데 그 문장은 여전히 참이라
(촬영은 오늘 탭에서도 상태 줄에서도 된다) 손대지 않았다 — 앱정보를 MCP로 고치는 경로는
아직 못 믿는다(T-014).

## 2026-09-02 · 첫 화면을 「제품」으로 — 얼굴은 부르기 전엔 안 보인다 (v4-1)

실기기 피드백 「들어가자마자 내 얼굴이 보인다」. 오늘 탭이 첫 화면이라 오늘 찍은 사진이 맨 위
가장 큰 표면이었다. 시안 셋(오늘 한 줄 · 대시보드 헤더 · 목록만+플로팅)을 보고 **A에 C의
「n일째」 숫자 블록을 섞기로** 했다.

- 탭 순서 제품 · 오늘 · 기록, 시작 탭 제품. **오늘 탭은 한 글자도 안 바뀐다** — 불만은
  「부르지 않았는데 보인다」이지 「보인다」가 아니다.
- 제품 탭 맨 위에 **사진 없는 오늘 상태 줄**(찍기 / 다시 찍기). 아침 알림을 받고 들어온 사람이
  한 번 눌러 촬영으로 간다. 어느 상태에도 `<img>`가 없음을 테스트가 잠근다.
- 카드는 버튼 셋을 잃고 **카드 자체가 버튼**이 됐다 — 「n일째」 블록(시작 당일 = 1일째, 종료면
  쓴 날수) + 메타 한 줄 + 셰브론. 종료·삭제는 카드를 눌러 연 폼 안으로.
- 「제품 추가」는 제목 줄의 글자 버튼. 빈 목록에서만 큰 버튼이 선다(언제나 0개 또는 1개).
- 겸사겸사 `VERDICT_KO`가 Home·History에 두 벌이던 것을 storage 한 벌로.

리뷰에서 셋을 고쳤다 — 카드 전체를 버튼으로 만들면서 **스크린리더가 「토너 수정」 한 마디만 듣게 된** 회귀를
`aria-describedby`로 되살렸고, 「n일째」 숫자의 `--blue`가 연파랑 위에서 **3.3:1(AA 미달)**이라 `--blue-dark`(4.8:1)로
내렸으며, 상태 줄 테스트 3건이 **DB 로드 전 프레임으로 통과**하던 것을 조였다(`shot`을 뒤집으면 6건 전부 죽는다).
실기기 검증은 다음 번들에서.

## 2026-09-02 · iOS 날짜 입력란이 카드 밖으로 넘치던 것을 고쳤다

실기기 스크린샷에서 제품 등록 폼의 **시작일 칸이 카드 오른쪽 경계를 넘어가고 값이 가운데
정렬로** 떴다. 원인은 iOS Safari의 date input이 **자체 고유 너비를 써서 `width:100%`를 무시**하고,
값을 shadow 요소(`::-webkit-date-and-time-value`)로 그려 **인라인 `textAlign`이 안 먹기** 때문이다.
전역 CSS 두 규칙으로 잡았고, 같은 코드를 쓰는 **수정 폼의 종료일 칸도 함께** 해결된다.
jsdom은 이 렌더링을 재현하지 못하므로 **실기기 확인은 다음 번들에서** 한다.

## 2026-09-02 · v3-1 라이브 출시 (`20260901-12`)

검수 승인 → 사용자가 콘솔 웹 「출시하기」 → **라이브**(08:18 · `deployed: true` 실측).
**검수 무반려 5연속.** 백업·복원이 실사용자에게 닿았다.

앞 항목의 ⚠️(낡은 콘솔 상세 설명)도 **해결**됐다 — 사용자가 출시하러 들어간 김에 콘솔
웹에서 두 글자를 고쳐, 라이브 값이 「'제품' 탭에서 기록 백업을 켜면」이 됐다.
⚠️ 다만 **`miniapp_update_basic_info`를 아직 못 믿는다**: 성공 응답을 주고도 값을 안 바꿨다.
앱정보를 MCP로 고치면 `miniapp_get`으로 **되읽어 확인**하고, 안 바뀌었으면 콘솔 웹으로 간다.

다음 계단 v3-2는 **코드가 0이다** — 두 번째 기기에서 익명 키 지문을 대조해 이 설계의
최대 미검증 가정(기기가 달라도 같은 키)을 확정하거나, 아니면 복원 코드 계단을 신설한다.

## 2026-09-01 · v3-1 검수 제출 (`20260901-12`)

실기기 검증을 마치고 검수를 요청했다(`REVIEWING`). 릴리스 노트에 백업이 **「제품」 탭**
맨 아래라는 것을 적었다 — 검수자를 옳은 자리로 보내는 안내이자, 아래 문제의 완충이다.

⚠️ **콘솔 상세 설명이 낡은 채로 남았다.** 「'오늘' 탭에서 기록 백업을 켜면」인데 백업은
「제품」 탭으로 옮겼다. `miniapp_update_basic_info`가 **성공 응답을 주고도 값을 안 바꿨고**
(`miniapp_get`은 옛 문구, `reviewState`도 `APPROVED` 그대로), `review_submit`은 반려·재검토용이라
APPROVED 상태에서 쓰면 앱정보를 불필요하게 검토 대기로 돌린다. 콘솔 웹에서 두 글자만
고치는 것이 가장 싸다. 개인정보 문장(사진은 기기에만·제품과 관찰만 서버로·끄면 삭제)은
**전부 여전히 참**이라 검수 리스크는 낮다.

## 2026-09-01 · 끄는 경로를 상태 뒤에 숨기지 않는다

경로를 적고도 **여전히 못 껐다.** 그 줄을 「켜짐」 결과일 때만 그렸는데, 실기기에서 요청이
`unavailable`로 실패하자 **경로가 통째로 사라졌다** — 화면에는 주황 오류 줄과 「받기」
버튼만 남았다.

두 가지가 틀렸다.

- 끄고 싶은 사람에게 **「먼저 켜기에 성공해야 끄는 법을 알려준다」**는 앞뒤가 안 맞는다.
- 애초에 앱은 켜졌는지 **알지도 못한다**(조회 API 없음). 즉 「켜짐일 때만」이라는 조건이
  성립하지 않는다 — 몇 주 전에 켠 사람은 앱을 열어도 영영 그 조건에 안 걸린다.

상태와 무관하게 늘 보이게 했다. 켜기 **전에** 보이는 것도 이롭다 — 빠져나갈 길을 알고 켠다.

## 2026-09-01 · 알림 끄는 경로를 단계까지 적는다

「토스 알림 설정에서 언제든 끌 수 있어요」라고만 적었더니 실기기에서 **못 껐다.**
주체만 말하고 **어디서인지를 안 말했다.**

하필 이 기능은 앱 안에 끄는 수단이 **하나도 없다**(SDK에 철회 API가 없다). 토스 설정의
그 항목은 네 단계 안쪽이라, 경로를 안 주면 「할 수 있다」가 사실상 「못 한다」였다.

- 「끄려면 토스 앱 → 전체 → 설정 → 알림 → 서비스별 알림」으로 바꿨다.
- ⚠️ 짐작으로 적으면 안 되는 값이라 **토스 공식 문서(스마트 발송 가이드)**에서 확인해
  옮기고 출처를 코드 주석과 테스트에 남겼다. 같은 문서가 「알림 동의문에 '알림 동의 철회
  경로'를 명확히 포함하는 걸 권장해요」라고 적고 있다 — 놓친 것이 권장 사항이기도 했다.

되돌리는 경로를 못 찾는 것은 켜는 경로를 못 찾는 것보다 나쁘다 — 사용자가 자기가 켠 것에
갇힌다([T-013](claude-docs/troubleshooting/T-013.md)).

## 2026-09-01 · 알림 결과를 화면이 말한다 — 뭉갠 값을 되살려서

실기기에서 「받기」를 눌렀는데 화면이 안 변해 **켜진 건지 아닌지 알 수 없다**는 보고.
T-010과 같은 반이다 — 명시적으로 누른 행동의 결과가 조용하면 그건 깜깜한 것이다.

표시가 약한 것보다 **값을 잃은 것**이 근본이었다. SDK는 `newAgreement` / `alreadyAgreed` /
`agreementRejected` **세 갈래**를 주는데 래퍼가 boolean으로 좁혀, 「방금 켰다」와 「원래
켜져 있었다」가 같은 값이 되어 있었다. 하필 이 SDK에는 **상태 조회 API가 없어서**
(`Notification`에는 `requestAgreement` 하나뿐 — 실측) `alreadyAgreed`가 앱이 진실을 아는
**유일한 순간**이었다. 그걸 버렸으니 이미 켠 사용자에게 해 줄 말이 처음부터 없었다.

- 래퍼가 갈래를 **그대로 흘린다**(`NotifyResult`). 좁히는 판단은 쓰는 화면이 한다.
- 오류·미지원은 거절과 **갈랐다**(`unavailable`) — 사용자가 안 켠 것과 우리가 못 물어본
  것은 화면이 할 말이 다르다. 못 물어본 것을 「안 켰다」로 적으면 거짓말이다.
- 화면은 넷을 각각 말한다: 「알림을 켰어요」 · **「이미 켜져 있어요」** · 「켜지 않았어요」 ·
  「지금은 열 수 없어요」(주황).

⚠️ 설계 전제 자체는 맞았다 — **렌더 시점**엔 상태를 모른다. 거기서 「상태를 말할 수 없다」로
한 칸 넓게 결론낸 것이 화근이었다. 누르는 시점엔 안다([T-012](claude-docs/troubleshooting/T-012.md)).

## 2026-09-01 · 아침 알림도 같은 행 모양으로 — 단, 스위치는 아니다

백업이 「제품」 탭으로 빠지자 「오늘」 탭에 고스트 버튼 하나만 남아 더 도드라졌다.

**같은 스위치로 만들지 않았다.** 동의의 단일 출처는 토스이고 사용자가 토스 설정에서
철회해도 앱은 알 수 없다(설계 §3-5) — 상태를 그리면 **동의한 사람에게도 앱을 열 때마다
「꺼짐」**으로 보인다. 화면이 거짓말을 하느니 모양만 같은 가족으로 두고, 오른쪽은 상태가
아니라 **행동**(`받기`)으로 남겼다.

바꾸는 김에 이 컨트롤이 안 하던 말 둘을 하게 했다.

- **언제 오는지** — 「매일 아침 8시에 찍을 시간을 알려드려요」. 받을지 말지를 고르는 데
  필요한 유일한 정보인데 여태 없었다.
- **끄는 곳이 앱 밖이라는 것** — 신청 직후 「토스 알림 설정에서 언제든 끌 수 있어요」.
  안 말하면 앱에서 끄는 법을 찾다 못 찾는다(앱에는 없다).

자리는 「제품」 탭과 같은 규칙이다 — **설정성 컨트롤은 목록 뒤, 구분선 아래.**

## 2026-09-01 · 백업 토글을 「제품」 탭 스위치로

실기기에서 마지막 백업 시각은 잘 떴는데 **컨트롤이 이질적**이었다. 「오늘」 탭 밑에 고스트
버튼 하나가 떠 있는 모양이었고, 그 탭이 답하는 질문(**오늘 찍었는가**)과 아무 상관이 없었다.

- **자리**: 「제품」 탭 목록 **뒤**. 백업이 지키는 것이 제품·관찰이라 대상 옆이고, 첫 등록
  때 뜨는 1회 제안 카드도 같은 탭이다. 목록 앞이 아닌 이유는 한 번 켜면 다시 만질 일이 없는
  설정이라 이 탭의 주 행동(제품 추가) 위에 상주할 이유가 없어서다.
- **형태**: 고스트 버튼 → 스위치(`role="switch"` + `aria-checked`). T-010의 원인이던
  **라벨 모호함이 구조로 사라진다** — 스위치에는 「켜기」 같은 행동 문구가 없다.
- **문구**: 상태 줄이 상태를 되풀이하지 않고 **왜 켜는지**를 말한다(「기기를 바꿔도 제품과
  관찰이 남아요」). 단 **켜 놓고 안 되는 상태**만은 주황으로 남겼다 — 지켜진다고 믿는데
  아무것도 안 올라간, 이 기능에서 제일 위험한 상태라 회색이면 정상처럼 읽힌다.

## 2026-09-01 · v3-1 태스크 6(1/2) — 문구 갱신과 번들 (검수 전)

「서버 0」이 끝나면서 **기존 문구 두 개가 거짓이 됐다.** 문구를 더하는 것보다 이게 중요하다.

- 온보딩 「카메라는 얼굴 사진을 찍는 데에만 쓰고, **계정도 서버도 없어요**」 → 서버가 생겼다.
  로그인이 없다는 사실은 그대로라 그 절반만 남겼다(「로그인도 계정도 없어요」).
- 온보딩 「제품을 검색할 때만 식약처 공공 데이터를 조회해요. **나가는 건 검색어뿐이에요**」 →
  백업을 켜면 제품·관찰도 나간다. 뒷문장을 걷었다.
- 콘솔 상세 설명 「**얼굴 사진과 기록은** 휴대폰에만 저장돼요」 → 기록은 서버에 갈 수 있다.
  「얼굴 사진은 휴대폰에만」으로 좁히고 백업 절을 붙였다.

**`LOCAL_ONLY`(사진 문장)는 글자 하나 안 바뀌었다** — 검수 통과 문구이자 온보딩 고지이고,
그게 이 설계의 성과다(§3-8). 사진은 여전히 기기를 떠나지 않는다.

테스트를 **옛 문장의 부재**에도 걸었다. 문구를 더하는 것은 기억나지만 지우는 것은 잘 잊고,
남은 옛 문장은 새 문장과 정면으로 모순된다 — 그게 검수에서 제일 나쁜 모양이다.

콘솔 상세 설명은 500자 제한에 걸려 497자로 맞췄다. **줄인 것은 기능 설명이고 개인정보
문단은 그대로 뒀다** — 줄일 곳을 고르는 것이 곧 무엇이 중요한지를 정하는 일이다.

크라우드소싱 고지는 **아직 넣지 않았다.** 설계 §3-8이 백업과 함께 묶어 뒀지만 그 기능은
v3-3이라, 지금 적으면 없는 기능을 있다고 말하는 것이 된다.

번들 `20260901-6` 빌드·테스트 푸시 완료. 실기기 확인과 검수 제출이 남았다 —
⚠️ **`User.getAnonymousKey`가 실기기에서 처음 불린다**(지금까지 전부 목이었다).

## 2026-09-01 · v3-1 태스크 5 — 백업·복원 UX 배선

복원 화면 · 온보딩 복원 링크 · Home 백업 토글 · 첫 등록 1회 제안 · 디바운스 업로드 ·
dirty 재시도. TDD로 Red 19건을 보고 Green으로 닫았다 — 테스트 443 → **468**, 번들 249.78kB.

**복원 화면의 값은 데이터가 아니라 고지에 있다.** 복원은 로컬을 통째로 덮어쓰는데 넘어오지
않는 것이 있다 — 사진이다. 그걸 복원한 **뒤에** 알게 되면 사고라서, 「사진은 복원되지
않아요」가 확인 버튼보다 **DOM 상 앞에** 있는지를 테스트가 잰다. 문구가 아래로 내려가는
변경은 테스트가 막는다.

숨기는 조건을 「켰는가」가 아니라 **「쓸 수 있는가」**로 잡은 것도 규율이다(알림 버튼과
동형) — 제안·버튼·복원 링크 셋 다 `isBackupSupported()`로 가린다. 키를 못 얻는 기기에
버튼만 남기면 「눌렀는데 아무 일도 없다」가 된다.

디바운스는 **마운트 직후 한 번을 그냥 지나간다.** 안 그러면 앱을 열기만 해도 5초 뒤에 안
바뀐 상태를 올린다. 백업을 켜는 순간에도 같은 이유로 다시 세운다 — 켤 때는 즉시 한 번
올리므로(켠 직후 서버가 비어 있으면 켠 보람이 없다) 디바운스까지 돌면 같은 것을 두 번 올린다.

복원은 저장소에 쓴 뒤 **다시 읽어서** 화면에 올린다. 서버에서 온 값이라 형태를 못 믿는데,
방어 규칙을 복원 쪽에 또 쓰면 로더와 두 벌이 되어 드리프트한다 — 있는 방어를 그대로 태운다.

설계에 없던 판단 둘을 적어 둔다: **복원하면 백업이 자동으로 켜진다**(복원했다는 것은
쓰겠다는 뜻이고, 새 기기에서 다시 켜게 만들 이유가 없다). 그리고 **Home 버튼은 알림과 달리
상태를 그린다** — 알림 동의의 단일 출처는 토스라 앱이 몰라야 하지만, 백업 켜짐의 단일
출처는 이 기기다. 켜 놓고도 확인할 자리가 없으면 「내 기록이 지금 지켜지고 있나」에
답할 방법이 없다.

## 2026-09-01 · v3-1 태스크 4 — 클라 백업 클라이언트 (다시 미니앱으로)

`logic/backup.ts`와 storage 플래그 넷. TDD로 Red 17건을 보고 Green으로 닫았다 —
테스트 405 → **443**, 번들 243.86kB.

이 모듈의 규율은 notify.ts와 동형이다: SDK가 던지는 것을 삼키고 **어떤 실패도 밖으로
던지지 않는다.** 서버가 죽어도 앱은 v2와 똑같이 돌아야 하기 때문이다. 그래서 테스트의
절반이 「실패해도 안 던진다」를 확인한다 — 500·429·네트워크 단절·파싱 실패 전부.

가장 값진 자리는 `buildBackupBlob`이다. **페이로드를 만드는 유일한 자리**이고, 최상위 키가
정확히 넷뿐임을 테스트가 잠근다 — 「사진은 이 기기에만 저장되며 어디로도 전송되지
않습니다」의 클라이언트 절반이 여기다. 서버의 2,000자 상한이 2차 방어라면, **보낼 코드를
아예 안 갖는 것**이 1차 방어다.

`getBackupKey`의 `try`가 둘인 이유가 서로 다르다는 것도 적어 뒀다 — `isSupported()`는 토스
웹뷰 전역을 읽어 **토스 밖에서 TypeError로 터지고**(notify가 이미 치른 값), `getAnonymousKey()`는
미지원 버전·브릿지 오류를 **던진다**(SDK 문서 확인).

설계를 하나 정정했다. **서버 URL을 소스에 박았다** — 초안은 `VITE_API_BASE`를 필수로 적었지만,
공개 값인데 환경변수를 필수로 두면 `.env`가 없는 빌드에서 백업이 **조용히** 죽는다(모든 서버
경로가 무음 폴백이라 아무도 못 알아챈다). MFDS 키는 비밀이라 env가 맞지만 이건 아니다.

⚠️ 그리고 `tsc`가 vitest가 못 잡은 것을 잡았다 — 테스트가 `Verdict`에 없는 `'good'`을 썼는데
`npm test`는 통과했다. **테스트 통과만으로는 부족하고 빌드까지 봐야 한다.**

## 2026-09-01 · v3-1 태스크 3 완료 — **facefit 서버 가동 시작**

`https://facefit-api.booktimer.app`이 라이브다. 「서버 0」 시대가 끝났다.

배포 순서를 지켰다 — BookTimer 레포의 Caddy 블록을 **먼저** 머지·배포해 그 호스트가
502(=DNS·Caddy·인증서까지 살아 있고 업스트림만 없음)가 되는 것을 확인한 뒤, facefit을
배포해 200으로 바꿨다. Caddy 덕에 인증서 발급·갱신에 우리가 한 일은 0이다.

**첫 배포는 실패했다.** OIDC 역할 assume가 `Not authorized`로 거부됐는데, 신뢰 정책도
공급자도 ARN도 다 맞아 보였다. CloudTrail에서 실제 `sub` 클레임을 꺼내 보고서야 원인이
드러났다 — GitHub이 facefit에는 **숫자 ID가 박힌 새 형식**
(`repo:Goospel@80618017/facefit@1350348409:...`)을 발급하는데, BookTimer 것을 복사한
신뢰 정책은 옛 형식만 알고 있었다. 같은 계정·같은 날인데 두 레포의 형식이 달랐다(T-007).
⚠️ 같은 이유로 **BookTimer 역할도 시한폭탄**이다 — 그쪽이 새 형식으로 바뀌면 배포가 멈춘다.

검증은 전부 실측이다. 스모크에서 **78,578바이트 페이로드가 바이트 단위로 그대로 왕복**해,
H2가 못 잡던 `MEDIUMTEXT` 회귀가 실제 MySQL에서 닫혔다. 한글과 미지 필드도 보존됐고,
2,001자 문자열은 400으로 막혔다 — **사진 밀반입 차단이 운영에서 작동한다.**

복구 리허설도 마쳤다. 첫 시도는 덤프에서 facefit 구간만 잘라 복원했다가 한글이 깨져 잠깐
백업을 의심했는데, 원인은 덤프 헤더의 `SET NAMES utf8mb4`를 함께 잘라낸 것이었다(T-008).
덤프를 **수정 없이 통째로 로컬 Docker의 빈 MySQL에** 넣으니 라이브 응답과 바이트 단위로
같았다 — 운영 박스에 두 번째 MySQL을 띄우지 않고도 실제 복구 절차와 같은 모양이 됐다.

**그리고 내 예측 하나가 측정으로 뒤집혔다.** 「BookTimer 배포 창에 물리 메모리를 ~200MB
넘긴다」를 최대 리스크로 적어 뒀는데, 근거가 BookTimer compose 주석의 **추정치**였다.
실측은 app 388MB·mysql 240MB로 한참 낮고 facefit은 149MB(추정의 절반)라, 배포 창에도
약 520MB가 남는다. 남의 문서의 추정치를 실측처럼 인용한 것이 오판의 원인이라 설계 §8에
지우지 않고 남겼다.

## 2026-09-01 · v3-1 태스크 3(1/2) — 동거 배선과 AWS 선행 (실배포 직전까지)

AWS 선행 작업을 CLI로 직접 만들고 검증했다. A 레코드 `facefit-api.booktimer.app`(TTL 60 —
나중에 분리할 때 전환을 1분으로 줄이는 유일한 준비다) · SSM 파라미터 3개 · ECR 리포지토리 ·
GitHub 시크릿 4개.

설계와 두 군데 달리했다. **SSM 이름을 `/facefit/db-password`가 아니라
`/facefit/SPRING_DATASOURCE_*`로** 만들었다 — BookTimer가 `/<프로젝트>/<환경변수명>` 꼴을 쓰고
있어서, 같은 규칙이면 이름이 SSM·환경변수·Spring 프로퍼티 세 곳에서 글자 그대로 같아지고
매핑 표가 아예 필요 없어진다(BookTimer는 이름이 어긋난 것들 때문에 매핑 표를 유지하고 있고
그 표가 유일 출처다). 그리고 **OIDC 역할을 기존 것에 얹지 않고 `facefitDeployRole`을 새로
만들었다** — 기존 역할에 facefit을 신뢰 대상으로 추가하면 facefit CI가 BookTimer 배포 권한을
통째로 갖는다. 새 역할은 ECR `facefit` 리포지토리와 S3 `deploy-facefit/` 접두어로 좁혔다.

코드는 Dockerfile · `compose.facefit.yaml`(별개 compose 프로젝트로 `booktimer_default`
네트워크에 external 참여 · `mem_limit 320m` · **ports 없음**) · `deploy-on-ec2.sh` ·
배포 워크플로다. **blue/green을 안 만든다** — 전환 순간 컨테이너가 2개가 되는데 이 박스는
BookTimer가 이미 그럴 때 물리 메모리를 넘긴다. 대신 워크플로 마지막의 라이브 헬스체크가
Caddy·DNS·인증서·컨테이너·DB를 한 번에 통과해야만 성공으로 친다.

선행 확인에서 하나 건졌다 — `backup-mysql.sh`가 `booktimer`에 하드코딩이라 확장을 안 받지만,
`--databases`에 `facefit`을 **덧붙이면** 백업 경로가 하나로 유지된다. 그 스크립트 주석이
「백업이 엿새 동안 조용히 0건이었다」는 실패를 기록하고 있어, 경로를 둘로 늘리지 않는 쪽이 맞다.

함정 1건: SSM 명령 JSON에 한글을 넣으면 AWS CLI가 Windows 로케일로 읽어 디코딩에 실패한다(T-006).

남은 것은 실서버 단계다 — BookTimer 레포 PR(Caddy 블록)이 **먼저** 배포돼야 헬스체크가 통과한다.

## 2026-09-01 · v3-1 태스크 2 — backup API (불변식의 서버측 집행)

`PUT`/`GET`/`DELETE /v1/backup`. TDD로 Red 23건을 먼저 보고 Green 38건으로 닫았다.

이 계단의 값은 엔드포인트가 아니라 **관문**에 있다. 「사진 바이트는 서버에 올라가지 않는다」는
불변식을 지금까지는 클라가 사진을 넣을 코드를 안 갖는 것으로만 지켰는데, 이제 서버가
구조적으로 막는다 — **재귀 문자열 길이 상한 2,000자**다. 사진 1장의 base64가 약 27만 자라
어떤 필드 이름으로 위장하든, 얼마나 깊이 숨기든 통과할 수 없다. 값뿐 아니라 **객체 키까지**
보는 것도 같은 이유다(값만 보면 긴 문자열을 키 자리에 넣는 것을 못 막는다).

저장은 **원본 바이트 그대로** 한다. 파싱한 트리를 다시 직렬화하면 공백과 키 순서가 바뀌어
「클라의 미지 필드를 있는 그대로 왕복 보존한다」는 계약이 깨진다 — v4 클라가 넣은 필드가
백업을 한 번 왕복하는 것만으로 모양이 달라지고, 그 사실은 복원한 뒤에야 조용히 드러난다.
본문을 `String`이 아니라 `byte[]`로 받는 것도 짝이다(512KB를 실제 바이트로 재고, 한글 해독을
컨버터 기본 문자셋에 안 맡긴다).

원 키는 어디에도 저장·기록하지 않고 sha256만 남긴다 — DB가 새도 열람 권한은 안 새야 한다.
레이트리밋은 IP당·키당 둘 다 건다(서로를 대신하지 않는다: 키당만 있으면 가짜 키 대량 생성으로
우회되고, IP당만 있으면 한 사용자가 남의 몫까지 태운다). `/health`는 일부러 제외했다 —
감시가 자기 때문에 빨간불이 되면 최악의 계측기다.

Boot 4에서 또 하나 밟았다: `-webmvc` 스타터가 **Jackson을 안 끌고 오고**, 딸려 오는 것은
**Jackson 3**(`tools.jackson.*`)라 `isTextual()`·`textValue()`가 `isString()`·`stringValue()`로
바뀌어 있다. Jackson 2 코드는 컴파일이 안 된다. 설계 §4-1에 박아 뒀다.

남은 구멍 하나는 정직하게 적었다 — H2는 `MEDIUMTEXT`도 `TEXT`도 CLOB이라 **크기 회귀를 못
잡는다.** Testcontainers를 들이는 대신, 태스크 3의 실통신 스모크에 64KB 넘는 페이로드 왕복을
넣어 실제 MySQL에서 확인하기로 했다.

## 2026-09-01 · v3-1 태스크 1 — 서버 부트스트랩 (첫 Java 코드)

`server/`에 Spring Boot 골격을 세웠다. `/health`(바디 없음 — 감시 도구가 상태 코드만 본다) ·
프로퍼티 기반 CORS(운영 2오리진, dev에서만 vite 5320 추가) · Flyway V1(backup 테이블) ·
CI 워크플로. TDD로 Red 8건을 먼저 보고 Green 12건으로 닫았다.

설계에 적힌 의존성 이름이 **Boot 3 것이라 그대로 쓰면 안 됐다** — BookTimer가 Boot 4.1.0을
쓰고 있어 `spring-boot-starter-webmvc`·`spring-boot-flyway`로 맞췄다. 두 서버가 같은 호스트에
동거하는 이상 조합도 같아야 운영자가 하나만 기억한다. 같은 이유로 Gradle 래퍼·툴체인·액션
버전도 BookTimer를 따랐다.

부수 소득 둘. **Flyway V1이 H2 MySQL 모드에서 그대로 적용돼** Testcontainers 승격이 아직
필요 없다(단 H2는 `MEDIUMTEXT`↔`TEXT` 회귀를 못 잡아, 그 판단은 태스크 2의 512KB 경계로
미뤘다). 그리고 CI에 **경로 필터를 안 뒀다** — 두면 문서-only PR에서 job이 스킵돼 required
check가 pending으로 남고 PR을 영영 못 머지한다(BookTimer가 이미 치른 값).

## 2026-09-01 · v3 서버 설계 확정 — 「서버 0」 종료, 그리고 BookTimer EC2 동거

OCR 무료 경로가 전멸했다는 실측(SDK에 OCR API 없음 · 토스 웹뷰가 라이브 텍스트 차단 ·
입력칸 콜아웃에 「텍스트 스캔」만 없음 · iOS에 카메라 앱 URL 스킴 없음)이 서버까지 밀어붙였다.
서버가 하는 일은 둘뿐 — 수기 등록 제품명의 익명 수집(크라우드소싱)과 제품·관찰 기록의
백업·복원. **사진 바이트는 서버에 절대 안 올라간다**는 불변식을 페이로드 상한 + 재귀 문자열
길이 상한으로 구조적으로 집행한다(`LOCAL_ONLY` 문장이 글자 그대로 계속 참이어야 한다).

설계는 두 번 뒤집혔다. 초안(Render+Neon 무료)이 사용자 결정으로 AWS 신규 인스턴스가 됐고,
**AWS를 CLI로 직접 재 보니 그 전제가 또 틀렸다** — 이 계정엔 프리티어가 없고(6~8월 청구에서
gross=net, 8월 $32.80 실결제) 서울에 t3.small이 이미 돌고 있었다. CPU는 평균 2%에 크레딧
만점, 디스크는 24GB 여유인데 **빠듯한 것은 메모리뿐**이라는 비대칭이 결론을 만들었다:
기존 박스 동거는 추가 0원, 신규 인스턴스는 월 ~$15.9.

동거가 지운 것이 크다. 리버스 프록시가 **Caddy**라 HTTPS가 자동이어서 certbot·타이머·갱신
실패 이중 감시가 통째로 사라졌고(구판이 「최대 함정」이라 부르던 항목이다), DB가 **MySQL 8.4**라
Postgres 전제를 폐기하고 데이터베이스 하나만 추가한다. 인스턴스·EIP·보안그룹·IAM 역할·swap·
부트스트랩 스크립트도 전부 이미 있는 것으로 대체됐다.

대가는 메모리 공유 하나이고 `mem_limit 320m`이 「facefit이 죽어도 BookTimer는 산다」를
보장한다. 늦게 발견한 제약도 정직하게 적었다 — BookTimer의 blue/green 배포로 app이 2개 뜨는
창에 물리 메모리를 ~200MB 넘긴다(무음 폴백이 흡수하지만 실측은 태스크로 남겼다).

**되돌리는 길을 지금 적었다** — 결합 목록 7개 · 분리 신호 5개 · 절차 5단계 · 지금 해 두면
그때 싸지는 준비 4개(A 레코드 TTL 60초 등). plan.md에도 인프라 현황 표로 옮겼고, BookTimer
레포에도 동거 문서와 `CLAUDE.md` 포인터를 별도 PR로 냈다 — 그쪽 세션이 AWS 장애를 디버깅할 때
정체 모를 컨테이너에 시간을 쓰지 않도록.

## 2026-08-30 · v2-3 라이브 출시 — 자연어 검색 실기기 검증까지

번들 `20260830-4`가 검수 즉시 승인(반려 0회 — **4연속 무반려**), 01:43 라이브 전환.
실기기에서 「토리든 다이브인 세럼」 검색이 실제로 그 제품들을 띄워 마지막 미검증
가정(전수 창 numOfRows 비표준 값의 실 API 수용)까지 해소됐다. v2-2 출시 → 사용자
피드백 → v2-3 설계·구현·리뷰·출시가 한나절 안에 돈 사이클. v2-3 계단 종료.

## 2026-08-30 · v2-3 똑똑한 검색 — 브랜드+제품명 자연어 (구현 완료 · 릴리스 전)

v2-2 라이브 직후 사용자 피드백(「내가 쓰는 제품명을 치면 그 제품이 떠야」)이 계기.
품목명에 브랜드가 없어 「토리든 다이브인 세럼」이 0건이던 것을, 최장 토큰 앵커 검색 +
나머지 토큰의 품목명·업소명 클라 대조로 해결했다(실측: 픽스처 17건에서 토리든 세럼
3건만 정확히 생존). 대조 술어는 스냅샷 유지 판정과 한 벌로 통합 — 브랜드 줄여 쓰기도
스냅샷이 산다. 하우스 브랜드(설화수→아모레퍼시픽) 한계는 전수 창 폴백으로 흡수.
리뷰가 v2-2의 「EE_NAME 사망」 판정이 표본 편향임을 실측으로 뒤집어(두 필드 상보),
effects 소스에 OR 편입 — 뱃지 공백 47%가 해소됐다. 검색 1회 1~4요청 상한, 번들
+0.57kB, 테스트 405. 함께 조사한 비기능성 커버리지 리서치는 「무료 우회로 전멸(네이버
쇼핑 API 2026-07 종료 실측)·수기 등록이 정답」으로 마감. 남은 것: 릴리스 후 실기기
자연어 검색 확인.

## 2026-08-29 · v2-2 라이브 출시 — 검수 무반려 3연속

번들 `20260829-3`(콘솔 상세 설명 「서버 전송 없음」→「제품 검색 시에만 식약처 공공
데이터 조회」 갱신 + 릴리스 노트에 네트워크 신설 선제 고지 포함)이 검수 약 10분 만에
승인, 23:57 라이브 전환. 실기기에서 「토너」 검색 제안이 떠 **실서비스 오리진 CORS**
(설계의 마지막 미검증 고리)까지 검증 완료 — v2-2 계단 종료.

## 2026-08-29 · v2-2 화장품 제품 API + 제품 표시 개선 — 구현 완료 (릴리스 전)

「디자인 개선의 핵심 = 제품 표시」라는 요구의 재료로 식약처 기능성화장품 보고품목
API(19만 건)를 붙였다. 핵심 반전: 리서치의 「프록시 없인 자동완성도 없다」 전제가 실측
2건(게이트웨이 CORS 반사 허용 · 앱인토스 외부 fetch 무제한)으로 뒤집혀 **클라이언트
직호출** 채택 — 운영 서버 0 유지, 대신 「런타임 네트워크 호출 0」이 처음 깨졌다(전송은
검색어뿐, 키 노출은 피해 상한 = 일 쿼터 소진으로 수용). 등록 폼의 이름 칸이 곧
검색창(2요청 최신순 — 기본 정렬이 2008년부터라)이고, 제안을 고르면 업소명·기능성
뱃지·SPF/PA가 스냅샷으로 박제돼 카드 2행에 선다. 뱃지는 법정 분류의 명사 인용만(§3-4 —
효능 문장 금지), 모든 실패는 무음으로 수기 입력 수렴. 코드 전 실측이 설계 가정 5곳을
고쳤고(EFFECT_YN 사망 → 고시 문구 감지 등), 리뷰 돌연변이 실측에서 공허한 계측기
3건을 잡았다. 테스트 294 → 378. 남은 것: 태스크 7(콘솔 문구 갱신 → 검수 → 실기기
CORS 최종 확인).

## 2026-08-29 · v2-1 촬영 리마인드 푸시 — 콘솔 예약 + 앱 동의 동선 (실기기 검증 전)

서버 0 그대로 리마인드를 붙였다 — 콘솔 REGULAR 정기 템플릿(AI 자동검수 4항목 즉시
APPROVED · 매일 08:00 · 문구 「아침 한 장 / 세안 후 오늘 얼굴을 남겨봐요.」 한 번에 통과)이
토스 대신 발송하고, 앱은 동의만 요청한다(첫 촬영 완료 직후 1회 제안 + Home 상시 버튼,
동의 상태 사본 없이 토스가 단일 출처). `@apps-in-toss/web-framework`가 처음 런타임
그래프에 들어와 번들 +61.5kB(gzip +18.6kB) — 기능상 필수 비용. 남은 것: 릴리스(새 번들
검수) 후 실기기 동의 → 다음날 08:00 실수신(templateCode 연결의 최종 검증).

## 2026-08-29 · v1 라이브 출시 🎉

번들 `20260829-1`이 첫 제출 **7분 만에 승인**됐다(INITIAL 검수 · 반려 0회 — restfit이
탭바로 3회 왕복한 전례와 대비. 검증된 수치를 복사해 온 전략이 통했다). 사용자가 콘솔 웹
「출시하기」를 눌러 19:48 라이브 전환(`deployed: true`). 구상 → 설계 → 구현 → 출시까지
하루에 끝났다. v1 계단 종료 — 다음은 plan ⏸절(v2)에서 고른다.

## 2026-08-29 · 첫 검수 제출 (`20260829-1`)

실기기 확인이 끝나(카메라 정상, `FLASH_MS`·`BASE_FPS` 조정 없이 확정) 번들
`20260829-1`을 릴리스 노트와 함께 검수에 넣었다 — `REVIEWING`. restfit의 첫 출시가
탭바 하나로 3회 왕복한 전례가 있어, 탭바는 그때 확정된 플로팅 캡슐 수치를 복사해 왔다.
반려되면 사유는 `review_get`이 아니라 `review_get_feedback`에서 본다(restfit 실측).
승인돼도 자동 출시는 아니다 — 콘솔 웹 「출시하기」는 사용자가 누른다.

## 2026-08-29 · 화장품 제품 데이터 API 리서치 — v2 계단에 반영

한국 출시 화장품의 제품 단위 공개 API가 있는지 조사해
[문서로 남겼다](docs/2026-08-29-cosmetics-data-api-research.md). 전 제품 API는 구조적으로
없고(품목 등록제가 아님), **1순위는 식약처 기능성화장품 보고품목 API**(무료·20만 건 —
v2 2단계 프록시에 얹어 제품명 자동완성), 성분 사전은 식약처 원료성분정보(1단계 번들 내장).
코리안넷 표준DB는 바코드 건당 유료 협의제라 후순위 — 공공포털 무료 바코드 API들이
2018년부터 동결된 것도 대한상의의 유료 트랙 회수 때문임을 확인했다. 구상 §5-7과
plan ⏸절에 링크를 걸었다. 코드 변경 0.

## 2026-08-29 · 콘솔 앱 생성 + 첫 번들 테스트 푸시 (태스크 10 전반)

콘솔에 미니앱을 등록했다 — miniAppId **70438**, `appName: facefit`(중복 없이 확보,
`intoss://facefit`), 카테고리 생활>일상>뷰티, 앱정보 검토는 제출 즉시 APPROVED.

아이콘은 restfit 파이프라인 그대로 — `icon.svg` 단일 출처(파인더 프레임 + 어제의 고스트
윤곽 + 뺨 반짝임, 토스 블루 계열) → `render-icon.ps1` Chrome 래스터화 → 600px 업로드.
업로드 검증은 HTTP 200 + 전송 바이트 = 파일 크기.

**개인정보처리방침은 콘솔에 별도 등록 자리가 없다**(설계 §6의 가정과 달랐다).
MCP의 `miniapp_update_privacy_policy`는 토스로그인 약관 전용이고, restfit도 별도 방침
문서 없이 검수를 통과했다 — 로그인·수집 0인 앱은 「기기 전용」 고지를 앱 안(온보딩)과
상세 설명에 싣는 것으로 충분하다. 상세 설명 마지막 줄에 「휴대폰에만 저장, 서버 전송
없음」을 박았다.

테스트 262건 그린 확인 후 `npm run release`로 번들 `20260829-1`(62,757바이트)을
업로드·컴파일(CREATED)하고 테스트 푸시까지 발송했다. `ait build`가 deploymentId를
stdout에 그대로 찍어 줘서 번들 헤더를 열 필요가 없었다.

남은 것: 실기기 확인(`FLASH_MS`·`BASE_FPS` 체감) → 검수 제출 → 콘솔 웹 「출시하기」.

## 2026-08-29 · v1 구현 (태스크 1~9)

설계 문서대로 미니앱을 세웠다 — 촬영(고스트 + 셔터 플래시 + 저장 직후 관찰 1문항) ·
제품 CRUD · 월간 캘린더 · 타임랩스(제품 구간 바) · 온보딩 · 3탭 배선.

restfit에서 검증된 것(카메라 수명·캡처 파이프라인·달력 산수·사진 저장소·확대 금지 가드)은
그대로 이식하고, facefit 고유의 판단만 새로 짰다. 서버는 0대 — 사진은 IndexedDB,
제품·관찰은 localStorage, 런타임 네트워크 호출 없음.

남은 것은 태스크 10(콘솔 앱 생성·개인정보처리방침·실기기 확인)뿐이다.
`FLASH_MS`(500) · `BASE_FPS`(6)는 거기서 실기기 체감으로 확정한다.

## 2026-08-29 · v1 설계 문서

restfit 이식 범위·스키마·태스크 분해를 한 장으로 확정했다. 결정 7건(관찰 기록 탭 포함 ·
셔터 플래시 · 고스트 기준 = 기준 사진 · 3탭 · 타임랩스 조작 · 비교 화면 제외 · 레포 구조).

## 2026-08-29 · 서비스 구상

화장품이 효과가 있는지 사진으로 관찰한다 — restfit의 눈바디·고스트 시스템을 얼굴로 옮기는 구상 확정.
