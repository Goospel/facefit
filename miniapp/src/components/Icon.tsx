/**
 * 앱 전용 아이콘.
 *
 * **이모지를 쓰지 않는다** — 기기·OS마다 다른 그림이 뜨는 데다, 어느 앱에나 있는
 * 남의 그림이라 화면이 「아무거나 갖다 붙인 것」처럼 보인다.
 *
 * 24 그리드, 2px 라운드 스트로크. 색은 `currentColor`라 부모가 정한다
 * (탭 활성이면 파랑, 아니면 회색).
 *
 * ⚠️ **검수는 20px에서 한다.** 하단 탭이 그 크기라, 40px에서 예쁜 것보다 20px에서
 * 안 뭉개지는 게 중요하다. 획을 하나 더 넣고 싶어질 때 먼저 무너지는 것도 여기다.
 */

/**
 * 이름 → 그 아이콘을 이루는 path `d` 목록.
 *
 * ⚠️ 타입을 `Record<string, string[]>`로 넓히지 않는다 — 그러면 `IconName`이 그냥 `string`이 되어
 * 오타난 아이콘 이름이 컴파일을 통과하고, 화면에는 빈 사각형이 뜬다.
 */
export const ICONS = {
  /**
   * 얼굴(오늘 탭). **어깨선이 있어야 얼굴로 읽힌다** — 타원만 그리면 알이 되고,
   * 이목구비를 넣으면 20px에서 뭉개져 얼룩이 된다. 촬영 화면의 가이드와 같은 그림이라
   * 「저 탭이 저 화면을 연다」가 눈으로 이어진다.
   */
  face: ['M12 3.2a4.9 6.1 0 1 0 0 12.2 4.9 6.1 0 1 0 0-12.2z', 'M3.8 21c1.2-3.6 4.3-5.6 8.2-5.6s7 2 8.2 5.6'],

  /**
   * 화장품 병(제품 탭). **목이 있어야 병이다** — 몸통만 그리면 상자가 된다.
   * 어깨를 좁혀 「짜는 것」이 아니라 「따르는 것」으로 읽히게 했다.
   */
  bottle: ['M10 2.8h4v3.4h-4z', 'M9.6 6.2h4.8a3 3 0 0 1 3 3v9a3 3 0 0 1-3 3H9.6a3 3 0 0 1-3-3v-9a3 3 0 0 1 3-3z'],

  /**
   * 달력(기록 탭). 위 가로선이 이 그림의 전부다 — 없으면 그냥 사각형이고,
   * 고리를 그리면 20px에서 테두리에 붙어 뭉갠다.
   */
  calendar: ['M4.4 5.6h15.2a1.6 1.6 0 0 1 1.6 1.6v12a1.6 1.6 0 0 1-1.6 1.6H4.4a1.6 1.6 0 0 1-1.6-1.6v-12a1.6 1.6 0 0 1 1.6-1.6z', 'M2.8 10h18.4', 'M8 3.2v4M16 3.2v4'],

  /**
   * 셰브론(제품 카드 오른쪽). **카드 전체가 버튼이라는 유일한 신호**라 장식이 아니다.
   * 획 하나라 20px에서도 선다 — 원을 두르거나 획을 더 넣으면 여기서 먼저 뭉개진다.
   */
  chevron: ['M9 6l6 6-6 6'],
};

export type IconName = keyof typeof ICONS;

export function Icon({ name, size = 20 }: { name: IconName; size?: number }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      style={{ flexShrink: 0 }}
      aria-hidden="true"
    >
      {ICONS[name].map((d) => (
        <path key={d} d={d} />
      ))}
    </svg>
  );
}
