# plan — facefit v1

> 앞으로 할 일. 설계는 [`docs/2026-08-29-v1-design.md`](docs/2026-08-29-v1-design.md)가 단일 출처다 —
> 여기는 그 §10 태스크 분해를 체크박스로 옮긴 실행 계획이다.
> 완료 기록은 [`changeLog.md`](changeLog.md), 함정은 [`claude-docs/troubleshooting.md`](claude-docs/troubleshooting.md).

**범례** ✅완료 / 🔜다음 / ⬜예정 / ⏸의도적 보류(v2) / ⚠️리스크·전제

## v1 — 기록이 쌓이는 것까지

> **v1 완료 — 2026-08-29 라이브 출시.** 태스크 10까지 전부 끝났다. 진행 중인 다음 계단은 아래 v2-1.

- [x] ✅ **1. 부트스트랩** — restfit 골격 복사 + 이름·포트·appName + 작업 추적 3종
  - [x] `miniapp/` vite 골격(package.json `facefit-miniapp` · dev 5320 · tsconfig · index.html · index.css · ui.ts)
  - [x] `apps-in-toss.config.ts` — `appName: 'facefit'` · webView 플래그 3종 · camera 권한
  - [x] 이식(수정 0): `camera.ts` · `logic/capture.ts` · `logic/calendar.ts` · zoom-guard (+각 test)
  - [x] plan · changeLog · troubleshooting 분할 시스템 + README 내비게이터
  - [x] `storage.ts`의 `todayKey`만 선반입 — `calendar.test.ts`가 「오늘 키가 그 달 셀에 있다」로 그걸 건다
- [x] ✅ **2. photoStore 이식** — `facefit-photos` / `FacePhoto` 타입 (테스트 이식 후 구현)
- [x] ✅ **3. storage 신설** — 제품·관찰·온보딩 + `todayKey`
- [x] ✅ **4. 순수 로직** — `logic/products.ts` · `logic/timelapse.ts`
- [x] ✅ **5. FacePhoto 화면** — 고스트 + 플래시 캡처 + 저장 후 관찰 1탭
- [x] ✅ **6. Products 화면** — 목록·추가·수정·종료·삭제
- [x] ✅ **7. History 화면** — 월간 캘린더 · 날짜 시트 · 사진 모두 삭제
- [x] ✅ **8. Timelapse 화면** — 자동 재생 · 스크럽 · 2× · 제품 구간 바
- [x] ✅ **9. Onboarding + App 배선 + Home** — 3탭 · 전체화면 3종
- [x] ✅ **10. 검수 준비** — 콘솔 앱 생성 · 개인정보처리방침 · 릴리스 노트 · 실기기 확인
  - 이 태스크는 **구현 범위 밖**이다. `FLASH_MS`(500) · `BASE_FPS`(6)는 여기의 실기기 실측으로 확정한다.
  - [x] 콘솔 앱 생성 — miniAppId **70438** · `appName: facefit`(중복 없음) · 앱정보 검토 즉시 APPROVED · 카테고리 생활>일상>뷰티
  - [x] 아이콘 — `assets/icon/icon.svg` 신설 + restfit `render-icon.ps1` 이식(600·1024 렌더 → 콘솔 업로드)
  - [x] 개인정보처리방침 — **별도 등록 경로가 콘솔에 없음을 실측**(restfit 전례와 동일: 로그인·수집 0인 앱은 앱정보+번들 검수뿐). 기기 전용 문구는 상세 설명 마지막 줄 + 온보딩에 내장
  - [x] 첫 번들 `20260829-1` 업로드(CREATED · sdk 3.1.1) + 테스트 푸시 발송(`isTested`)
  - [x] 실기기 확인(사용자) — 카메라 정상 작동 확인, `FLASH_MS`(500)·`BASE_FPS`(6) 그대로 확정
  - [x] 검수 제출 — 번들 `20260829-1` REVIEWING(2026-08-29, 릴리스 노트 포함)
  - [x] 검수 결과 — 첫 제출 **7분 만에 APPROVED**(반려 0회 · restfit은 3회 왕복했었다) → 사용자가 콘솔 웹 「출시하기」 클릭 → **라이브**(2026-08-29 19:48, `deployed: true`)

## v2-1 — 촬영 리마인드 푸시 (🔜 진행 중)

> 설계 단일 출처: [푸시 리마인드 설계](docs/2026-08-29-push-reminder-design.md) (2026-08-29 사용자 승인).
> 서버 0 유지 — 콘솔 정기 발송(REGULAR·토스 대신 발송), 매일 08:00 KST 고정, 동의자 자동 세그먼트.

- [x] **1. 콘솔 — 알림동의문** — `termsId` **118527** (SCHEDULED · DAILY 08:00 · purpose 「촬영」, 동의 화면 제목 「매일 촬영 알림을 보내드릴까요?」 자동 생성 확인)
- [x] **2. 콘솔 — REGULAR 기능성 템플릿** — 생성·자동검수 4항목 통과 즉시 APPROVED = 매일 08:00 예약 완료(`templateSetGroupNo` **195487** · templateCode **`facefit-daily-photo-reminder`** — `{appName}-` 접두어 필수 실측). ⚠️ `push_test_send`는 3회 실측 실패(「메세지 템플릿이 없어요」 — 스마트메시지 경로 미지원 추정), 수신 검증은 태스크 6으로 일원화
- [x] ✅ **3. storage 확장** — `facefit.notifyPrompted` 게터/세터 (TDD · 4케이스)
- [x] ✅ **4. notify 래퍼 + FacePhoto 제안 스텝** — `isSupported` 가드 · 1회 제안 규율 (TDD · SDK mock · 12+9케이스).
  ⚠️ SDK `isSupported()`는 토스 웹뷰 전역(`window.__appsInTossConstants`)을 읽어 **토스 밖에서는 던진다** — 래퍼가 try로 삼킨다
- [x] ✅ **5. Home 「아침 알림 받기」 버튼** — 멱등 재동의 경로 겸용 · 동의 성공 시에만 세션 내 「알림 신청됨」 (TDD · 4케이스)
- [ ] ⬜ **6. 릴리스 + 실기기 검증** — 동의 시트 → 다음날 08:00 실수신(설계 §7-1 templateCode 가정의 최종 검증) → plan/changeLog sweep

## v2-2 — 화장품 제품 API + 제품 표시 개선

> **v2-2 완료 — 2026-08-29 라이브 출시**(`20260829-3` · 검수 무반려 3연속). 후속 v2-3은 아래 절.

> 설계 단일 출처: [제품 API 설계](docs/2026-08-29-product-api-design.md) (2026-08-29 사용자 승인 · 실측·리뷰 반영 3차 개정).
> 식약처 기능성화장품 보고품목 API **클라이언트 직호출**(CORS 반사·앱인토스 외부 fetch 무제한 실측 — 프록시 기각).
> 「운영 서버 0」은 유지, 「런타임 네트워크 호출 0」은 이번에 처음 깨짐(전송은 검색어뿐).

- [x] ✅ **0. 선행(사용자)** — data.go.kr 활용신청 즉시 자동승인(개발계정 · 만료 2028-08-29 · 기능별 일 10,000회), 키는 `miniapp/.env.local`(gitignore)
- [x] ✅ **1. 키 스모크 + 가정 실측** — 부분일치 확정(「토너」 5,948건) · 봉투 확정(0건 시 `items` 부재) · 어긋남 5곳 발견(EFFECT_YN 사망 → `EE_DOC_DATA` 감지 · 정렬 파라미터 없음 → 2요청 최신순 · 브랜드 0건 · PA 숫자 · 취하 필드) → 설계 개정
- [x] ✅ **2~3. `logic/mfds.ts`** — 파서(고시 문구 감지 · PA "4"→"++++" · 취하 제외) + 2요청 최신순 `searchProducts`(키 부재 즉시 `[]` · 전 실패 무음) (TDD · 실측 픽스처 4건)
- [x] ✅ **4. storage 확장** — `MfdsSnapshot`(itemName 포함) revive 표시용 등급(어긋나면 `mfds`만 떨굼)
- [x] ✅ **5. Products 폼 자동완성** — 이름 입력란 = 검색창(2자+ · 300ms 디바운스 · abort) · 명시 탭만 부착 · 이름 수정 시 토큰 규칙으로 스냅샷 조건부 유지
- [x] ✅ **6. 카드 메타** — 업소명 · 기능성 뱃지(명사만 — §3-4 규율) · SPF/PA 칩 · 출처 캡션 1회 · 온보딩 고지 한 줄. 리뷰(머지 가능 · Minor 5건 전부 반영 · 돌연변이 41종+ 사살) 완료 — 테스트 378 그린 · 번들 +3.7kB
- [x] ✅ **7. 문구 갱신 + 릴리스 + 실기기 검증** — 콘솔 상세 설명·온보딩 갱신 + 릴리스 노트 네트워크 선제 고지 → 번들 `20260829-3` 검수 즉시 승인(반려 0회) → 라이브(2026-08-29 23:57) → **실기기 「토너」 검색으로 실서비스 오리진 CORS 최종 검증 통과**(설계 §7-3 마지막 미검증 고리 해소)

## v2-3 — 똑똑한 검색: 브랜드+제품명 자연어

> **v2-3 완료 — 2026-08-30 라이브 출시**(`20260830-4` · 검수 무반려 4연속 · 실기기 자연어 검색 검증).

> 설계 단일 출처: [똑똑한 검색 설계](docs/2026-08-30-smart-search-design.md) (2026-08-30 사용자 승인).
> 계기: v2-2 라이브 직후 사용자 피드백 「내가 쓰는 제품명을 치면 그 제품이 떠야」 — 품목명에 브랜드가 없어 「토리든」 0건.
> 함께: [비기능성 커버리지 리서치](docs/2026-08-30-product-coverage-research.md) — 무료 우회로 전멸 실측(네이버 쇼핑 API 2026-07 종료 등), 비기능성은 수기 등록이 정답으로 확정.

- [x] ✅ **0~4. 구현(TDD)** — 앵커 사다리(최장 토큰·2자+·3회 캡) + 대조 술어 한 벌(`hasAllTokens` — 품목명∨업소명, `keepsSnapshot` 공유·브랜드 줄여 쓰기 유지) + 창 이원화(T≤30 전수/광역 최신 30) + 폴백(전수 창 전멸 → 앵커 결과) + placeholder 「예: 토리든 다이브인 세럼」. 실응답 픽스처 17건 잠금
- [x] ✅ **리뷰 반영** — 머지 가능(Blocker 0) · 돌연변이 44+3종 사살 · Important 1(정규화 유틸 통합)·Minor(EE_NAME 상보성 실측 → effects 소스 OR 편입 — v2-2 설계 §4-1 추기, 뱃지 공백 8/17건 해소) 반영. 테스트 405 그린 · 번들 +0.57kB
- [x] ✅ **5. 릴리스 + 실기기 검증** — 번들 `20260830-4` 검수 즉시 승인(반려 0회) → 라이브(2026-08-30 01:43) → **실기기 「토리든 다이브인 세럼」 검색 확인**(전수 창 numOfRows 비표준 값의 실 API 수용 — 마지막 미검증 가정 해소)

## v3 — 첫 서버: 기록 백업 + 크라우드소싱 제품 DB (🔜 다음)

> 설계 단일 출처: [서버 설계](docs/2026-08-30-server-design.md)
> (2026-08-30 작성 · **2026-09-01 AWS 실측으로 호스팅·DB 전면 개정**).
> 계기: OCR 무료 경로 전멸 실측 — 앱 안에서 제품명을 자동으로 뽑는 길이 없다(설계 §2-1).
> 남은 두 길이 이 서버의 두 목적이다 — **다른 사용자가 이미 친 이름 재사용**(크라우드소싱) ·
> **기기 이전 시 기록 보존**(백업).
> **불변식**: 사진 바이트는 서버에 절대 안 올라간다 — `LOCAL_ONLY` 문장이 글자 그대로 계속 참이어야 한다.

### 🏗️ 인프라 현황 — 「서버 0」의 종료 (2026-09-01 실측·결정)

> **2026-09-01 가동 시작** — `https://facefit-api.booktimer.app` 라이브(health 200).

**facefit 서버는 자기 인스턴스가 없다 — BookTimer의 EC2에 얹혀 산다.**
이 절은 나중에 「왜 이렇게 돼 있지?」와 「언제·어떻게 떼어내지?」에 답하기 위한 것이다.
상세는 설계 §2-5(실측) · §3-6(호스팅) · **§3-6-1(분리 경로)**.

| 항목 | 값 |
|---|---|
| AWS 계정 | `459338751419` — **CLI 프로필 `booktimer`가 유일한 관리자**. ⚠️ `default` 프로필은 **다른 계정**(`681689809064`)의 CI 사용자라 대부분 권한 거부(「없다」로 오독하기 쉽다) |
| 인스턴스 | `i-07a649585c25707a3` — t3.small · 서울 · EIP `15.165.95.129` · **BookTimer 소유** |
| 리버스 프록시 | `caddy:2-alpine` — HTTPS 자동(certbot 없음). facefit vhost는 **BookTimer 레포**의 `deploy/caddy/Caddyfile` |
| DB | `booktimer-mysql-1`(mysql:8.4) **재사용** — `facefit` 데이터베이스 + 전용 사용자(권한은 그 DB에만) |
| 도메인 | `facefit-api.booktimer.app` → 위 EIP (Route 53 `Z0795663J1W7C48SU27B` · **TTL 60초**) |
| facefit 자원 몫 | 컨테이너 1개 · `mem_limit 320m` · `-Xmx160m` — **실사용 149MB**(상한의 46%, 2026-09-01 실측) |
| 배포 | facefit 레포 GitHub Actions → ECR `facefit` → SSM. **`facefitDeployRole`**(ECR·S3 접두어로 좁힘) |
| 추가 비용 | **0원** ⚠️ 단 이 계정에 **프리티어가 없다**(8월 $32.80 실결제) — 분리하는 날이 곧 월 ~$15.9가 시작되는 날 |

**⚠️ 분리 신호** — 하나면 판단, 둘이면 분리(절차는 설계 §3-6-1 ③):
호스트 여유 메모리 **300MB 미만 지속** · facefit 컨테이너 **OOM 재시작 이력** ·
MySQL 슬로우 쿼리 · BookTimer 배포가 서로를 흔듦 · facefit 트래픽이 BookTimer와 같은 자릿수.

**지금 해 두는 분리 준비 4가지**(안 지키면 그때 설계를 다시 해야 한다):
① A 레코드 TTL 60초 ② 전용 MySQL 사용자·DB ③ compose 조각을 facefit 레포에
④ DB 접속 정보 환경변수 주입.

**측정으로 정정된 것(2026-09-01)**: 「BookTimer 배포 창에 물리 메모리를 ~200MB 넘긴다」는
예측은 **틀렸다.** 근거로 삼았던 1.78GB가 BookTimer compose 주석의 **추정치**였는데, 실측은
app 388MB · mysql 240MB로 한참 낮다. facefit(149MB)까지 얹고 blue/green으로 app이 하나 더
떠도 약 **1,389MB / 1,913MB**다. **남의 문서의 추정치를 실측처럼 인용한 것이 오판의 원인**이라
적어 둔다 — 같은 실수를 다음 계단에서 반복하지 않기 위해서다.

**BookTimer 쪽에도 남긴다** — 그쪽 세션이 AWS 장애를 디버깅할 때 정체 모를 `facefit-api`
컨테이너에 헤매지 않도록, BookTimer 레포에 동거 문서 + `CLAUDE.md` 포인터(태스크 3-e).

### v3-1 — 백업·복원 (가장 작은 첫 조각)

> **v3-1 완료 — 2026-09-02 라이브 출시**(`20260901-12` · 검수 무반려 5연속 · 실기기 5차 왕복으로
> 알림 UX를 세 번 고쳤다). 다음 계단은 아래 v3-2 — 코드 0, 사용자 실기기 몫이다.

- [x] ✅ **1. 서버 부트스트랩** — `server/` Gradle 골격 + `/health` + CORS + Flyway V1(backup 테이블) + CI 워크플로. TDD Red(12개 중 8개 실패) → Green(12개 통과).
  - 실측 정정: 설계 §4-1의 의존성 이름은 **Boot 3 이름**이었다. BookTimer(Boot 4.1.0)에 맞춰 `spring-boot-starter-webmvc`·`spring-boot-flyway`로 고쳤다 — 두 서버가 같은 조합이어야 운영자가 하나만 기억한다.
  - Flyway V1이 **H2 MySQL 모드에서 그대로 적용**돼 Testcontainers 승격은 아직 불필요. ⚠️ 단 H2는 `MEDIUMTEXT`↔`TEXT` 회귀를 못 잡는다(둘 다 CLOB) — 태스크 2의 512KB 경계에서 재판단.
  - CI는 **경로 필터 없이** 단일 워크플로다 — 필터를 두면 문서-only PR에서 job이 스킵돼 required check가 pending으로 남고 PR을 못 머지한다(BookTimer가 같은 자리에서 배운 것).
- [x] ✅ **2. backup API** — PUT/GET/DELETE. TDD Red(38개 중 23개 실패) → Green(38개 통과 · 스킵 0).
  - 걸린 경계: 왕복 · 404 · DELETE 멱등 · LWW · **2,000자 문자열 400(값뿐 아니라 객체 키까지)** · 512KB 초과 400 · 최상위 키 밖 400 · products 200 · notes 4,000 · 미지 필드 왕복 보존 · 키 없음/짧음/긺 401 · **원 키 비저장(DB에 sha256만)** · 키당·IP당 429 · `/health`는 레이트리밋 밖
  - 실측 정정: Boot 4의 `-webmvc`가 **Jackson을 안 끌고 오고**, 딸려 오는 것은 **Jackson 3**(`tools.jackson.*`)라 메서드 이름이 다르다(`isTextual`→`isString` 등). 설계 §4-1에 반영.
  - 저장은 **원본 바이트 그대로** 한다 — 파싱한 트리를 다시 직렬화하면 공백·키 순서가 바뀌어 「있는 그대로 보존」이 깨진다. 본문도 `String`이 아니라 `byte[]`로 받는다(512KB를 실제 바이트로 재고, 문자셋을 컨버터 기본값에 안 맡긴다).
  - ⚠️ **H2가 못 잡는 것 하나**: `MEDIUMTEXT`↔`TEXT` 회귀(둘 다 CLOB). Testcontainers를 들이는 대신 **태스크 3-h 스모크에 64KB 넘는 페이로드 왕복을 넣어** 실제 MySQL에서 확인한다.
- [x] ✅ **3. 동거 배선 + 배포 + 스모크** — **2026-09-01 서버 가동 시작.** `https://facefit-api.booktimer.app` 라이브
  - [x] ✅ 3-a 선행 확인 3가지 — 도커 네트워크 **`booktimer_default`** · 배포 자산 `/opt/booktimer/` · `render-env.sh`의 SSM↔환경변수 매핑. `backup-mysql.sh`는 `booktimer` 하드코딩이라 **`--databases booktimer facefit`으로 한 단어만 덧붙인다**(백업 경로를 둘로 늘리지 않는다 — T-138에서 이미 치른 값)
  - [x] ✅ 3-b AWS 선행 — A 레코드(INSYNC·공개 DNS 확인) · SSM `/facefit/SPRING_DATASOURCE_{URL,USERNAME,PASSWORD}`(설계의 `db-password` 대신 **BookTimer 관례에 맞춤** — 이름이 곧 환경변수라 매핑 표가 불필요) · ECR `facefit` · **`facefitDeployRole` 신설**(기존 역할에 얹지 않았다 — 그러면 facefit CI가 BookTimer 배포 권한을 통째로 갖는다) · GitHub 시크릿 4개
  - [x] ✅ 3-c MySQL `facefit` DB + 전용 사용자 — `GRANT ALL ON facefit.*`만(BookTimer 데이터엔 못 닿는다). **백업 스크립트에 `facefit`을 넣기 전에** 만들었다 — 순서를 뒤집으면 그날 밤 cron이 실패한다
  - [x] ✅ 3-d `server/deploy/compose.facefit.yaml` + `Dockerfile` + `deploy-on-ec2.sh` — `mem_limit 320m` · `-Xmx160m` · external 네트워크 · **ports 없음**
  - [x] ✅ 3-e **BookTimer 레포 PR 머지·배포** — Caddy 블록 + `backup-mysql.sh` 한 단어 + 동거 문서. 로컬에서 `caddy validate`(실 이미지)·그쪽 자체 테스트 2종 통과 후 머지. 배포 직후 facefit 호스트가 **502**(=DNS·Caddy·인증서까지 살아 있고 업스트림만 없음)인 것을 확인
  - [x] ✅ 3-f 배포 워크플로 작성 — 테스트 → ECR → S3(`deploy-facefit/`) → SSM → **라이브 헬스체크**. blue/green 없음(메모리 — 설계 §5). 실동작 검증은 머지 시
  - [x] ✅ 3-g DB 백업 + **복구 리허설 완료** — 백업 실행 → S3 객체 확인(덤프에 facefit 포함) → **로컬 Docker의 빈 MySQL에 덤프를 통째로 복원** → 한글 보존 + **라이브 API 응답과 바이트 단위 동일** 확인 → 정리. ⚠️ 첫 시도는 덤프를 잘라 복원해 한글이 깨졌다 — 백업이 아니라 복원 방법의 문제였다(T-008)
  - [x] ✅ 3-h 실통신 스모크 + 메모리 실측 — health 200 · 404 · PUT/GET 왕복(**한글·미지 필드 바이트 보존**) · **78,578바이트 왕복 동일**(H2가 못 잡던 `MEDIUMTEXT` 회귀를 실제 MySQL에서 확인) · DELETE 204→404 · 401 · **2,001자 400(사진 밀반입 차단이 운영에서 작동)** · CORS 허용/차단(403). 메모리: facefit **149MB**(상한 320) · 호스트 `available` 754MB · OOM 0 · 재시작 0
- [x] ✅ **4. 클라 `logic/backup.ts` + storage 확장** — TDD Red(17건) → Green. 테스트 405 → **443**, 번들 243.86kB
  - `buildBackupBlob`이 **페이로드를 만드는 유일한 자리**다 — 최상위 키 넷이 정확히 그것뿐임을 테스트가 잠근다(불변식의 클라이언트 절반: 사진을 **보낼 코드가 아예 없다**)
  - `getBackupKey`의 `try`가 둘인 이유가 다르다 — `isSupported()`는 토스 밖에서 **TypeError로 터지고**, `getAnonymousKey()`는 미지원 버전·브릿지 오류를 **던진다**
  - 플래그 넷은 전부 **꺼진 쪽이 안전한 기본값**. `backupEnabled`·`backupDirty`만 양방향(끄지 못하면 삭제권 입구가 없고, 지우지 못하면 영영 재시도한다)
  - 실측 정정: **서버 URL을 소스에 박았다**(설계는 `VITE_API_BASE` 필수였다). 공개 값인데 env 필수로 두면 `.env` 없는 빌드에서 백업이 **조용히** 죽는다 — 무음 폴백이라 아무도 못 알아챈다
  - ⚠️ `tsc`가 vitest가 못 잡은 것을 잡았다 — 테스트가 `Verdict`에 없는 `'good'`을 썼는데 테스트는 통과했다. **`npm test`만으로는 부족하고 `npm run build`까지 봐야 한다**
- [x] ✅ **5. 클라 UX 배선** — TDD Red(Restore 8건 + App 11건) → Green. 테스트 443 → **468**, 번들 249.78kB
  - 복원 화면의 값은 데이터가 아니라 **고지**다 — 「사진은 복원되지 않아요」가 확인 버튼보다 **DOM 상 앞에** 있는지를 테스트가 잰다(뒤로 가면 복원한 뒤에 알게 된다)
  - 숨기는 조건은 「켰는가」가 아니라 **「쓸 수 있는가」** — 제안·버튼·복원 링크 셋 다 `isBackupSupported()`로 가린다(눌러도 아무 일 없는 표면을 안 만든다)
  - 디바운스 효과가 **마운트 직후 한 번은 그냥 지나간다** — 안 그러면 앱을 열기만 해도 안 바뀐 상태를 올린다. 켤 때도 같은 이유로 다시 세운다(즉시 업로드와 겹치지 않게)
  - 복원은 저장소에 쓴 뒤 **다시 읽어서** 화면에 올린다 — 서버 값의 방어를 `loadProducts`/`loadNotes`에 그대로 태운다(규칙을 두 벌로 만들지 않는다)
  - 설계에 없던 판단 둘: 복원하면 **백업을 자동으로 켠다**(복원했다는 건 쓰겠다는 뜻이다) · 백업 토글은 알림 버튼과 달리 **상태를 그린다**(켜짐의 단일 출처가 이 기기라서). ⚠️ 처음엔 Home에 뒀다 — 실기기 지적으로 제품 탭 스위치가 됐다(아래 태스크 6)
- [ ] 🔜 **6. 문구 갱신 + 릴리스 + 실기기 검증**
  - [x] ✅ 온보딩 문구 — **낡게 된 두 문장을 걷어냈다**: 「계정도 서버도 없어요」(서버가 생겨 거짓) · 「나가는 건 검색어뿐이에요」(백업이 생겨 거짓). 로그인이 없다는 사실만 남기고 백업 고지를 신설. `LOCAL_ONLY`는 **글자 하나 안 바뀐다**(설계 §3-8의 성과). 테스트가 옛 문장의 부재까지 잰다 — 더하는 것보다 **안 지운 옛 문장**이 위험하다
  - [x] ✅ 콘솔 상세 설명 — 「얼굴 사진과 기록은 휴대폰에만」이 거짓이 되므로 「얼굴 사진은 휴대폰에만」으로 좁히고 백업 절 추가(497자/500 — 개인정보 문단은 그대로 두고 기능 설명을 줄였다)
  - [x] ✅ 번들 `20260901-6` 업로드·빌드 완료 + 테스트 푸시
  - [x] ✅ **실기기 1차 — 가정 통과, 버그 1건 발견** — 백업 UI가 떴고(=`isSupported` 참) **PUT·DELETE 프리플라이트가 실제로 나갔다** = `User.getAnonymousKey`가 키를 돌려줬다(설계 §8-1의 최대 가정 통과). 다만 전부 **403**이었다 — CORS 오리진이 설계의 `private-web`이 아니라 **`private-apps`**였다(T-009). 앱은 무음 폴백이라 「눌렀는데 아무 일도 없음」으로만 보였고, **진단은 Caddy 액세스 로그**가 했다
  - [x] ✅ CORS를 **패턴으로** 교체 — `facefit.tossmini.com` · `facefit.*.tossmini.com`. 틀리면 무음으로 죽는 값을 「맞히기」로 두지 않는다(다른 미니앱 오리진은 계속 막힘, 테스트 2건 신설)
  - [x] ✅ **실기기 2차 — 끝까지 뚫렸다(2026-09-01 21:25)** — CORS 재배포 후 `PUT 200`, MySQL에 **1행**(231바이트 · 제품 1 · 관찰 1 · client→server **268ms**). 키는 **sha256만**(`6f167c981529…`) 저장. 한글도 정상(`LENGTH 231` vs `CHAR_LENGTH 217`, HEX가 「어성초 클렌징 폼」의 UTF-8 — 콘솔에 `???`로 보인 건 **출력 경로**였지 데이터가 아니었다)
  - [x] ✅ **UX 결함 2건 수정**(T-010) — 사용자가 「기록 백업 켜기」를 **이미 켜진 상태로 읽었다**(그래서 안 눌렀고, 나는 코드에서 원인을 찾고 있었다). 상태와 행동을 갈라 버튼 위에 상태 줄을 뒀다. 같은 줄이 두 번째 구멍(**켜 놓고 안 되는 상태가 화면에 안 보임**)도 막는다
  - [x] ✅ **백업 토글을 「제품」 탭으로 옮기고 스위치로 교체** — 실기기에서 마지막 백업 시각은 잘 떴지만 고스트 버튼이 **이질적**이라는 지적. 자리는 목록 **뒤**다(한 번 켜면 안 만지는 설정이라 주 행동 위에 상주할 이유가 없고, 1회 제안 카드가 이미 같은 탭에서 안내한다). 라벨 모호함(T-010)은 `role="switch"`+`aria-checked`가 구조로 없앤다 — 스위치엔 행동 문구 자체가 없다. 상태 줄은 상태를 되풀이하지 않고 **왜 켜는지**를 말하되, **켜 놓고 안 되는 상태만 주황**으로 남긴다
  - [x] ✅ **실기기 3차 — 스위치 확인 완료**(번들 `20260901-8`)
  - [x] ✅ **아침 알림도 같은 행 모양으로** — 백업이 빠지자 홈에 고스트 버튼 하나만 남아 더 도드라졌다. ⚠️ **스위치로는 못 만든다**: 동의의 단일 출처가 토스라 철회를 앱이 감지할 수 없고(설계 §3-5), 상태를 그리면 **동의한 사람에게도 매번 「꺼짐」**으로 보인다. 그래서 모양은 같은 가족(카드 행)이되 오른쪽은 상태가 아니라 **행동**(`받기`)이다. 겸사겸사 안 하던 말 둘을 한다 — **언제 오는지**(아침 8시)와 **끄는 곳이 앱 밖**이라는 것
  - [x] ✅ **알림 결과를 화면이 말한다**(T-012) — 실기기에서 「받기」를 눌러도 화면이 안 변해 **켜진 건지 모르겠다**는 보고. 근본 원인은 표시가 아니라 **값을 잃은 것**이었다: SDK가 주는 세 갈래를 래퍼가 boolean으로 좁혀 `alreadyAgreed`(= 지금 켜져 있음)를 버렸고, 이 SDK엔 **상태 조회 API가 없어** 그게 진실을 아는 유일한 순간이었다. 갈래를 그대로 흘리고(`NotifyResult`) 오류·미지원은 거절과 갈라(`unavailable`) 넷을 각각 문장으로 말한다. ⚠️ 「렌더 시점엔 모른다」는 맞았지만 거기서 「말할 수 없다」로 한 칸 넓게 결론낸 것이 화근
  - [x] ✅ **알림 끄는 경로를 단계까지**(T-013) — 「토스 알림 설정에서 끌 수 있어요」만 적었더니 실기기에서 **못 껐다**. 앱에 끄는 수단이 하나도 없는데(SDK에 철회 API 없음) 경로를 안 주면 「할 수 있다」가 사실상 「못 한다」다. 토스 공식 문서(스마트 발송 가이드)에서 확인해 「토스 앱 → 전체 → 설정 → 알림 → 서비스별 알림」으로 옮겼다 — 짐작 금지 값이라 출처를 주석·테스트에 남겼다
  - [ ] ⬜ (선택) **콘솔 알림 동의문에도 철회 경로 추가** — 토스 문서의 권장 사항. 동의문 수정은 콘솔 웹에서 해야 하고(MCP에 update 없음), 새로 만들면 기존 동의가 끊기므로 **급하지 않다**
  - [x] ✅ **끄는 경로를 상태 뒤에 숨기지 않는다**(T-013 후속) — 경로를 적고도 못 껐다. 「켜짐」 결과일 때만 그려서, 요청이 `unavailable`로 실패하자 경로가 통째로 사라졌다(주황 오류 줄 + 「받기」뿐). 앱은 켜졌는지 **알지도 못하므로** 그 조건 자체가 성립하지 않는다 — 상태와 무관하게 늘 보이게 했다
  - [x] ✅ **실기기 5차 — 끄는 경로 상시 노출 확인**(번들 `20260901-12`)
  - [x] ✅ **검수 제출**(2026-09-01 · `20260901-12` → `REVIEWING`) — 릴리스 노트에 백업이 **「제품」 탭**임을 명시해 검수자를 옳은 자리로 안내했다
  - [x] ✅ **콘솔 상세 설명 정정** — 「**'오늘' 탭에서** 기록 백업을 켜면」이 백업을 「제품」 탭으로 옮기며 거짓이 됐다. `miniapp_update_basic_info`는 **성공 응답 뒤에도 값을 안 바꿨고**(`miniapp_get` 옛 문구 그대로, `reviewState`도 `APPROVED` 유지), `review_submit`은 반려·재검토용이라 APPROVED 상태에서 쓰면 앱정보를 불필요하게 검토 대기로 돌린다 → **사용자가 콘솔 웹에서 두 글자를 고쳤다**(현재 라이브 값 `'제품' 탭에서 …` 확인). ⚠️ **앱정보를 MCP로 고치는 경로는 아직 못 믿는다** — 다음에 상세 설명을 바꿀 일이 생기면 호출 뒤 `miniapp_get`으로 **반드시 되읽어** 확인하고, 안 바뀌었으면 콘솔 웹으로 간다
  - [x] ✅ **검수 승인 → 라이브**(2026-09-02 08:18 · 사용자가 콘솔 웹 「출시하기」 클릭 · `deployed: true` 실측)

### v3-2 — 익명 키 가정 실검증 (코드 0 · 사용자 실기기 몫)

- [ ] ⬜ **7. 두 번째 기기에서 키 지문 대조** — 동일하면 가정 확정, 다르면 복원 코드 계단(v3-2.5)을 신설.
  ⚠️ **이 설계의 최대 미검증 가정**이라 v3-1 출시 문구는 복원을 과약속하지 않는다(백업 사실만 말한다).

### v3-3 — 크라우드소싱 제품 DB

- [ ] ⬜ **8. community API** — 등록 병합 · **2인 미만 비노출** · hidden · 토큰 AND 대조 · 10건 컷 (TDD)
- [ ] ⬜ **9. 클라 검색 병합** — MFDS 먼저·크라우드 뒤 · 「사용자 등록」 표시 · 뱃지 없음 · 수기 저장 시 fire-and-forget 전송
- [ ] ⬜ **10. 문구·릴리스**(익명 공유 고지) + 실기기 검증 → sweep

### ❓ 아직 못 정한 것

- ~~크라우드소싱 수집 방식~~ — **2026-09-01 결정: 수기 저장 시 자동 전송 + 고지**(설계 §3-5 권고안 채택). 고지는 온보딩과 콘솔 상세 설명 두 곳. 등록 폼에는 아무것도 안 붙인다 — 조작이 한 단계 늘면 기여율이 죽고, 기여량이 이 기능의 생사다.
- **두 번째 폰 확보 여부** — v3-2 검증의 전제. 없으면 그 계단이 통째로 막힌다.

## v4-1 — 첫 화면을 「제품」으로

> **v4-1 완료 — 2026-09-02 라이브 출시**(`20260902-14` · 검수 무반려 6연속 · 실기기 확인 후 제출 → 승인 → 13:55 라이브). 후속 번들 `20260902-15`도 같은 날 라이브(7연속).

> 설계 단일 출처: [첫 화면 설계](docs/2026-09-02-products-first-design.md) (2026-09-02 사용자 승인).
> 계기: 실기기 피드백 「들어가자마자 내 얼굴이 보인다」. 시안 A(오늘 한 줄) + C의 「n일째」 블록.

- [x] ✅ **1~6. 구현(TDD)** — `VERDICT_KO` 한 벌(storage) · `chevron`·`--blue-soft` · 사진 없는 오늘 상태 줄 ·
  탭 순서 제품·오늘·기록 + 시작 탭 products · 읽는 카드(「n일째」 블록, 시작 당일 = 1일째) + 종료·삭제를 폼 안으로 ·
  「추가」 글자 버튼(빈 목록에선 큰 버튼). 오늘 탭 0 변경. 어느 상태에도 `<img>`가 없음을 테스트가 잠근다
- [x] ✅ **리뷰 반영** — 카드 내용을 `aria-describedby`로 스크린리더에 되살림 · 「n일째」 글자 `--blue-dark`(4.8:1 — `--blue`는 3.3:1로 AA 미달이었다) · 비동기 계측기 3건을 로드 뒤 단정으로 조임(shot 반전 시 6건 전부 사망 확인) · 설계 규칙 2개 잠금. 테스트 501 그린 · 번들 255.10kB
- [x] ✅ **7. 릴리스 + 실기기 검증**
  - [x] ✅ 번들 `20260902-13`(iOS 날짜 칸 수정만) 업로드·테스트 푸시 → 실기기 확인(사용자) — 검수 제출 없이 `-14`로 대체
  - [x] ✅ 번들 `20260902-14` 업로드(87,045바이트 · 번들 255.10kB · sdk 3.1.1) · 테스트 푸시 → **실기기 확인(사용자)** — 첫 화면 제품 · 상태 줄 → 카메라 → 제품 탭 복귀 · 카드 → 폼 → 종료/삭제 · 오늘 탭 그대로
  - [x] ✅ **검수 제출**(2026-09-02 10:5x · `20260902-14` → `REVIEWING`) — 릴리스 노트에 첫 화면 변경·상태 줄·카드·추가 버튼 위치·iOS 날짜 칸 수정을 적어 검수자를 옳은 자리로 안내. 콘솔 상세 설명은 「첫 화면」 언급이 없어 **그대로 참**(수정 불필요 — T-014의 MCP 되읽기 함정도 안 밟는다)
  - [x] ✅ **검수 승인 → 라이브**(2026-09-02 13:55 · 사용자가 콘솔 웹 「출시하기」 클릭 · `deployed: true` 실측)

> 후속 후보(이번 범위 밖 · 리뷰 지적): `FacePhoto.tsx`의 `VERDICT_OPTIONS`가 관찰 문구의 세 번째 사본(한 줄로 `VERDICTS.map`에 접을 수 있다) · 미래 시작일/종료일에 「-6일째」·선계산 날수가 뜬다(날짜 input에 `min`/`max`가 없다 — main에서도 `D+-7`이었다).
> → **2026-09-02 처리**: 둘 다 같은 날 후속 PR로(사본은 `VERDICTS.map` 한 줄 · 미래 날짜는 `min`/`max` + 저장 조건으로 입력에서 막음).
> → 번들 `20260902-15` 테스트 푸시 → 실기기 확인(iOS 피커가 범위 밖을 막는 것) → 검수 제출 → 승인 → **라이브(2026-09-02 15:05 · `deployed: true` 실측 · 검수 무반려 7연속)**.

## v4-2 — 가끔 쓰는 제품의 실사용 기록 (🔜 진행 중)

> 설계 단일 출처: [사용 기록 설계](docs/2026-09-02-usage-frequency-design.md) (2026-09-02 사용자 승인 · 대안 B).
> 계기: 실기기 피드백 「팩같은건 일주일에 한두번인데 지금 서비스는 모든걸 매일 사용한다고 가정한다」.
> 로그는 사진의 속성 — `usage[사진 날짜] = 그 사진 직전 루틴에 쓴 제품 id 배열`. 3상(키 없음 / `[]` / `[id…]`).
> **⚠️ 배포 순서 고정: 서버(PR ①)가 라이브가 된 뒤에 클라(PR ②)를 머지한다.** 어기면 새 클라의 백업 PUT이
> 전부 400으로 밀린다 — 무음 폴백이라 손실은 없고 재시도로 회복되지만, 사용자가 주황 「아직 백업하지 못했어요」를 본다.

### 계단 1 — 로그가 쌓이고 카드·배지에 보인다 (PR 둘)

- [x] ✅ **PR ①(서버 · `feat/backup-usage-key`)** — `BlobValidator`에 최상위 키 `usage` 화이트리스트 ·
  `MAX_USAGE_DAYS = 4_000`(`MAX_NOTES`와 같은 논리) · 객체·키 수 검사. `usage` **안은 안 본다**(3상은 클라의 뜻).
  옛 클라 블롭(키 넷)은 그대로 200. 컨트롤러·리포지토리·마이그레이션 0 변경. 테스트 3건 추가(서버 45 그린)
  - [x] ✅ 머지 = main push → **`deploy.yml` 자동 배포**(테스트 게이트 → ECR → EC2 SSM → 헬스체크) →
    `PUT` 스모크(옛 블롭 200 · `usage` 포함 블롭 200)
  - [x] ✅ **2026-09-02 머지 → 자동 배포 success → 라이브 스모크**: health 200 · 옛 블롭(키 넷) PUT 200 ·
    `usage` 블롭 PUT 200 · `usage` 배열 400 · GET 왕복 225바이트 동일(한글 포함) · DELETE 204→404.
    **→ PR ②의 「서버 라이브 뒤에 머지」 조건 충족.**
- [x] ✅ **PR ②(클라 · `feat/usage-log`)** — `storage`(`frequency`·`Usage`·로더 3종) ·
  `logic/backup`(블롭 키 다섯 · `schemaVersion` 1 유지) · `App` 배선 · `FacePhoto` 칩 블록(두 출구 모두 `onUsage`) ·
  `Products` 폼 셀렉트 + 카드 「n회」 · `Timelapse` 프레임 배지.
  테스트 506 → 548(새 42건 중 Red 35건 · 돌연변이 9종 사살 — 구현 7 + 리뷰 2) · 번들 253.10 → 255.87kB.
  **①이 라이브인 뒤에 머지한다**
  - 설계와 다르게 판단한 둘: 안내 한 줄을 `<label>` **밖**에 뒀다(안에 두면 그 칸의 접근성 이름이
    「사용 빈도촬영할 때…」로 오염돼 `getByLabelText`가 못 찾는다 — **T-015**) · `FacePhoto.test`의
    `saveOk` 헬퍼를 모듈 스코프로 올려 두 describe가 나눠 쓴다(사본을 안 만들려고)
  - 리뷰 반영(Minor 3): 칩 묶음에 `role="group"` + `aria-labelledby`(칩 이름이 제품명 한 마디라
    묶음에 질문을 안 달면 무엇을 묻는지 못 듣는다) · 빈도 안내 줄을 `aria-describedby`로 이음
    (T-015 재발방지의 나머지 절반) · 이 줄의 기준선 수치 정정 — **510은 Red 상태의 통과 수**였지
    main 기준선(506)이 아니었다
- [ ] 🔜 **릴리스** — 서버 배포 확인 → 클라 머지 → 번들
  - [x] ✅ **클라 머지 + 번들 `20260902-16`** — 클라 PR 머지(2026-09-02) → `npm run release`(번들 257.88kB —
    키가 박힌 production 값이라 PR의 255.87kB보다 크다) → 업로드·빌드 CREATED(sdk 3.1.1) → 테스트 푸시 발송
  - [x] ✅ **실기기 검증**(설계 §5 절차) — 사용자 확인 완료(2026-09-02): 가끔 제품 등록·폼 안내 줄 → 촬영 시 칩 블록 →
    칩 켜고 답 → 카드 「1회」 → 타임랩스 배지 · 다시 찍기에서 칩이 켜진 채 뜸 · **매일 제품만 있으면 화면 0 변경** ·
    백업 PUT
  - [x] ✅ **라이브**(2026-09-03 13:40 · `20260903-19`로 함께 출시 — `-16`의 내용이 그대로 사용자에게 갔다)
  - [x] ⚠️ **`-16`은 2026-09-03 취소됨**(사용자 지시) — 21시간 넘게 「대기」라 `review_cancel`로 접고, 같은 내용을
    포함한 `20260903-19`로 다시 제출했다(v5 절). **취소한 번들은 영구히 재제출 불가**라 `-16`은 여기서 끝난다.
    지연의 원인은 끝내 확정하지 못했다 — 앱정보 간극(2026-09-03 정정)이 유력한 가설이었으나 정정 뒤에도 안 풀렸다
  - [x] ✅ **검수 제출**(2026-09-02 · `20260902-16` → `REVIEWING`) — 릴리스 노트: 「사용 빈도」 칸 · 촬영 뒤 「쓴 것」 체크 ·
    카드 「n회」 · 타임랩스 배지 · 백업에 사용 기록 포함 · 매일 제품만이면 화면 그대로.
    ⚠️ 콘솔 상세 설명을 `miniapp_get`으로 되읽은 결과 백업 문장이 「**제품과 관찰 기록만** 서버에 저장」이라
    사용 기록이 실리는 지금은 좁다 — ~~사용자가 콘솔 웹에서 고친다~~
  - [x] ✅ **콘솔 상세 설명 정정**(2026-09-03) — 위 간극이 **검수 지연의 원인**으로 의심됐다(이전 7건은 5~19분
    승인, `-16`은 12시간+ 「대기」 — 릴리스 노트가 「사용 기록도 저장」이라 말하는데 앱정보는 옛 범위).
    `miniapp_update_basic_info`로 고쳤다(495자): 백업 문장 「제품·관찰·사용 기록만」 · 촬영 경로 「'제품' 탭
    맨 위나 '오늘' 탭」(`-18`까지 참) · 「가끔」 제품 한 문장. 앱정보 검토 `IN_REVIEW` → 승인 → `miniapp_get`
    새 문구 확인. **T-014 추기** — 반영 판정은 `miniapp_get`이 아니라 `miniapp_meta_status.reviewState`
  - [ ] ⬜ **출시**(사용자가 콘솔 웹 「출시하기」) → `deployed: true` 실측

### 계단 2 — 대조가 보인다 (착수 전 설계 §4-8을 명세로 승격)

- [ ] ⬜ 타임랩스 바의 사용일 점(`logic/timelapse.ts` `marks`) · 기록 캘린더 날짜 시트에서 보기·고치기.
  서버 0 변경. **계단 1을 실기기로 일주일쯤 써 본 뒤 연다** — 배지만으로 대조가 읽히면 점은 안 찍는다.

## UX 개선 1차 — 스위치 · 촬영 화면

명세: [`docs/2026-09-02-ux-switch-camera-spec.md`](docs/2026-09-02-ux-switch-camera-spec.md). 원칙 둘을 실행했다 —
**켜짐과 꺼짐은 색·표식·글자 셋으로 가른다** · **화면에 파란 버튼은 하나**.

- [x] ✅ **아이콘 셋 추가**(`check`·`timer`·`layers`) — 24 그리드 · 2px 라운드 스트로크, 16px 이하에서 검수
- [x] ✅ **백업 스위치**(`Products.tsx`) — 트랙 색 + 트랙 안 체크/빈 원 + 아래 「켜짐/꺼짐」 글자,
  아래 줄 문장이 「켜져 있어요/꺼져 있어요」로 **시작**, 켜진 행은 카드 톤까지 `--blue-soft`
- [x] ✅ **알림 행 알약**(`Home.tsx`) — 오른쪽을 테두리 두른 알약으로(누르면 열린다는 신호),
  단 `on`일 때는 알약을 걷고 체크 + 「켜짐」. ⚠️ 「끄려면 토스 앱 → …」 경로 줄은 그대로
- [x] ✅ **촬영 컨트롤 한 줄**(`FacePhoto.tsx`) — 「촬영」·「3초 후 촬영」 두 버튼을 걷고
  `[3초 칩] (셔터) [겹치기 칩]`. 셔터가 화면에서 유일하게 큰 버튼이고, 겹치기 칩은 기준 사진이
  없으면 `visibility: hidden`으로 자리만 지킨다(셔터가 가운데 서야 한다 → T-016)
- [x] ✅ 테스트 548 → **558**(+10) · 돌연변이 7건 실측 사살 · `npm run build`(tsc) 통과
- [x] ✅ **리뷰 반영**(PR #37) — 켜짐 톤을 `borderColor`만 덮던 shorthand 혼용을 `History.tsx`
  `cellStyle` 방식으로 교정(끄는 순간 React가 테두리를 통째로 지웠다) · 표식·`aria-hidden`·
  전환 회귀에 계측기 3건 추가 · 촬영 칩 `aria-label`을 보이는 글자에 맞춤(WCAG 2.5.3)
- [x] ✅ **머지 + 번들 `20260902-17`**(main `7f061f6` · 260.21kB · sdk 3.1.1 · CREATED) → 테스트 푸시
- [x] ✅ **실기기 검증**(사용자 확인 2026-09-02) — 스위치 꺼짐/켜짐 그림 · 켰다 끈 뒤 카드 테두리 복귀 ·
  알림 알약 → 체크+켜짐 · 촬영 한 줄(셔터 가운데, 사진 0장이면 겹치기 자리만 비움)
- [x] ✅ **라이브**(2026-09-03 13:40 · `20260903-19`로 함께 출시)
- [x] ~~검수 제출~~ → **`-17`은 제출하지 않는다** — 2차가 같은 날 머지돼 `20260903-18`이 1차+2차를 한 번들로
  담았다(아래 UX 2차 절). 콘솔에 CREATED로 남아도 무해

## UX 개선 2차 — 첫 진입 · 찍기 CTA · 변화 보기

설계: [`docs/2026-09-02-ux-2-design.md`](docs/2026-09-02-ux-2-design.md)(2026-09-02 사용자 승인).
1차의 「파란 버튼은 하나」를 **화면 구조**에까지 밀었다 — 파란 자리가 **지금 할 일**을 따라 옮겨 다닌다
(제품 0개 → 「제품 추가」 / 제품 ≥1·안 찍음 → 「오늘 얼굴 찍기」 / 찍음 → 없음).

- [x] ✅ **제품 탭 부품 셋**(`Products.tsx`) — `TodayStrip`(행 전체가 버튼이던 상태 줄)을 걷고
  `StartCard`(첫 진입 · 할 일 둘을 번호로 · ①만 파랑 · `LOCAL_ONLY` 고지 신설) ·
  `ShootCta`(얼굴 아이콘 + 「오늘 얼굴 찍기」 · 카드 ② 자리에서는 회색 톤으로 재사용) ·
  `ShotRow`(체크 + 「오늘 찍었어요」 · **행 전체는 버튼이 아니고** 「다시 찍기」 하나만 ghost)
- [x] ✅ **기록 탭 변화 보기**(`History.tsx`) — 타임랩스 입구를 달력 **위**로 올리고 「재생」을 파란
  알약으로(썸네일 56×72 · 아래 줄을 「최근 M/D」에서 **기간**으로), 1장 안내는 달력 아래 그대로,
  「사진 모두 삭제」는 `<hr>`로 격리(보상과 파괴가 한 덩어리로 읽히던 자리)
- [x] ✅ **`play` 아이콘**(`Icon.tsx`) — 획 하나짜리 삼각형(16px에서 검수)
- [x] ✅ 테스트 558 → **566**(+8) · 「파란 버튼은 하나」를 세는 계측기로 세 상태에서 1·1·0 단언 ·
  돌연변이 5건 실측 사살(찍은 뒤 CTA 잔류 2죽음 · 카드 ② 파랑 · 카드를 달력 아래로 · 행 전체 버튼화 ·
  `hr` 제거) · `npm run build`(tsc) 통과
- [x] ✅ **리뷰 반영**(PR #40 · Minor 6건) — 566 → **572**(+6). 제품 0개+폼 열림에서 `LOCAL_ONLY`
  고지가 카드에 딸려 사라지던 구멍(고지를 카드 밖으로) · `md`(「8월 1일」) 3중복을
  `logic/calendar.ts` `formatMd`로 통합(Products·History·Timelapse) · 안 쓰는 `ui.empty` 삭제 ·
  첫 진입 카드 안 `ShotRow`는 `bare`로 카드 톤 제거(카드 안 카드) · 번호 원 색 계측기 1건
  (돌연변이: ①의 `now` 제거 → 1죽음)
- [x] ✅ **머지 + 번들 `20260903-18`**(main `a850859` · 261.99kB · sdk 3.1.1 · CREATED) → 테스트 푸시.
  1차+2차가 한 번들이라 `-17`은 대체됐다(제출 안 함)
- [x] ✅ **실기기 검증**(사용자 확인 2026-09-03) — 첫 진입 카드(①만 파랑 · 폼을 열어도 고지 잔류) ·
  찍기 CTA → 찍은 뒤 파란 버튼 0개 · 기록 탭 변화 보기 카드 + 파란 「재생」 · 삭제 격리 · 1장이면 안내만
- [x] ✅ **라이브**(2026-09-03 13:40 · `20260903-19`로 함께 출시)
- [x] ~~검수 제출~~ → **`-18`도 제출하지 않는다** — 같은 날 기름종이 알림이 더해져 `20260903-19`가 1차+2차+v5를
  한 번들로 담았다. `-19` 제출로 대체(아래 v5 절). `-17`·`-18`은 둘 다 사문(CREATED로 남는다)
- [ ] ⏸ **오늘 탭 축소**(오늘 사진 + 알림만 남기기) — 2차 뒤 두 탭이 같은 파란 버튼을 갖지만 원칙은
  「한 화면에 하나」라 충돌이 아니다. 실기기로 써 본 뒤 겹친다고 느껴지면 그때 연다(설계 §5)

## v5 — 얼굴 기름종이 알림 (🔜 진행 중)

설계: [`docs/2026-09-03-oil-paper-reminder-design.md`](docs/2026-09-03-oil-paper-reminder-design.md)(2026-09-03 사용자 승인 —
야간 상한 없음 · 익명 키 거부 시 토스 로그인 · 오늘 탭 배치). 체크 1회 = 알림 1회. 서버가 3시간 뒤 스마트 발송 API를 직접 호출한다.

- [x] ✅ **절차 1 · mTLS 인증서 발급**(사용자 · 콘솔 웹, 이름 `facefit-server` · 2026-09-03) → 레포 루트 `mTLS/`(gitignore)에 zip + PEM 2개.
  키는 **PKCS#8 그대로**(`BEGIN PRIVATE KEY` — openssl 변환 불필요). ⚠️ 만료일은 콘솔 웹에서 확인해 여기 적을 것: ____
- [x] ✅ **절차 2 · SSM 파라미터 4개** — `FACEFIT_REMINDER_KEK`(32B · PowerShell RNG) · `FACEFIT_REMINDER_TEMPLATE_SET_CODE` · `FACEFIT_TOSS_MTLS_CERT_PEM` ·
  `FACEFIT_TOSS_MTLS_KEY_PEM`(SecureString · 되읽어 로컬과 동일). ⚠️ Git Bash에서 `/facefit/…` 인자는 MSYS 경로 변환으로 「fully qualified name」
  오류 + describe 필터는 **조용히 빈 목록**(글로벌 원칙 5 재발) — PowerShell로 우회
- [x] ✅ **절차 3 · CONDITION_BASED 동의문** — `termsId` **119657**(제목 「기름종이를 쓰고 체크하면 다음 알림을 보내드릴까요?」)
- [x] ✅ **절차 4 · 스모크** — `openssl s_client -cert/-key`로 `anon-key/verify` → TLS 통과 · 200 · 4010 「인증 정보를 찾을 수 없어요」.
  인증서 없이는 TLS alert 116 「certificate required」 → **mTLS 유효 확정**. 4010은 헤더 유무·종류와 무관하게 같아 **익명 키 수용은
  실기기 진짜 키의 3시간 뒤 실수신으로만 확정**(아래 C-5). ⚠️ Windows curl(Schannel)은 PEM·PFX 전부 거부 — mTLS 스모크는 `openssl s_client`로
- [x] ✅ **S-1~S-4 서버** — 마이그레이션·KeyCipher·API·워커·메신저(TDD · 서버 테스트 89건) → PR
  - **다크런치**: KEK·템플릿 코드·mTLS PEM 중 하나라도 없으면 알림만 쉰다(PUT은 503 · 워커는 1회 경고 후 스킵).
    「PEM이 있는데 못 읽는」 경우(파일 삭제·권한·형식)도 같은 취급이다 — 번들을 실제로 만들어 보고 판정한다.
    그래서 절차 1~3보다 먼저 머지해도 안전하다 — 배포 스크립트도 새 SSM 파라미터가 없으면 경고만 남기고 넘어간다.
- [x] ✅ **C-1~C-4 앱** — storage·reminder 로직·notify 인자화·OilReminder 카드·랜딩(TDD) → PR.
  테스트 **630건**(기준선 572 + 신규 58). 리뷰 반영 1회 — 알약·표식을 `ui`로 한 벌 통합 · 연타 가드 ·
  자정 넘긴 예약 잠금. ⚠️ 서버가 라이브가 되기 전까지
  「썼어요」는 「지금은 예약할 수 없어요」로 끝난다(무음 폴백)
- [x] ✅ **절차 5~6 · SERVER 템플릿** — `push_template_create` type=SERVER → **AI 자동검수 즉시 APPROVED**(4항목 pass · `push_send_scheduled` 불필요).
  `templateSetGroupNo` **11588** · `templateSetCode` **`facefit-oil-paper-reminder`** · 문구 A안(「기름종이 시간 / 체크한 뒤 3시간이 지났어요.」 —
  본문에 「(그화장품효과있나 알림)」 자동 부착) · `linkUri intoss://facefit?tab=home`. ⚠️ SERVER는 **그룹과 소재의 발송 코드가 같아야 한다**
  (설계의 `-1` 접미 소재 코드는 거부 — v2-1 REGULAR와 다르다). 절차 7(`send-test-message`)은 생략 — 코드가 그룹=소재 하나라 모호함이 없다
- [x] ✅ **S-5 배포·스모크**(2026-09-03 12:46 KST) — main `11ac4b1` 자동 배포 성공 → `PUT /v1/reminder` 200 `{dueAt: now+180분}` · 키 없음 401 ·
  `DELETE` 204. EC2: `/opt/facefit/toss/client-*.pem` 600·uid 10001 · `.env`에 SSL 번들 2줄+알림 2줄 · Flyway V2 적용 · 다크런치 경고 없음.
  워커 발송 경로 검증용 **가짜 키 예약 1건**을 남겨 뒀다(15:49 KST 발송 시도 → 토스 4010 거부 로그가 곧 mTLS 발송 경로의 증거)
- [x] ✅ **C-5 번들·실기기(1차)** — 번들 `20260903-19`(main `fbf9152` · 265.88kB · sdk 3.1.1 · UX 1차·2차 포함 → `-18` 대체) 테스트 푸시 →
  **실기기 확인(사용자 2026-09-03)**: 카드 노출 · 「썼어요」 → 동의 시트(제목 = 동의문 `sendCondition`) 뜸. **시트는 닫았다(동의 안 함)** —
  그래서 서버에 진짜 키 행은 아직 없다(Caddy 접근 로그로 확인: 기기발 `/v1/reminder` 요청 0건 — 거절이면 서버를 안 부르는 설계대로).
- [ ] ⬜ **C-5 (2차) · 실수신 확인** — 사용자가 「썼어요」 → **동의** → 「예약됨」까지 간 뒤 3시간: 워커가 진짜 키로 발송 → 푸시 도착(=익명 키
  수용 최종 확정) → 탭 시 오늘 탭 랜딩 → 「다음 알림」 1회전. 서버 로그(`docker compose logs`)의 발송 결과가 1차 판정, 기기 수신이 최종.
  DB 확인은 SSM Run Command로 `mysql -u$SPRING_DATASOURCE_USERNAME …`(`.env`에서 `set -a; . ./.env`) — root는 비밀번호가 없어 거부된다
- [x] ✅ **`-16` 취소 + 상세 설명 갱신 + `-19` 검수 제출**(2026-09-03 · 사용자 지시) — 하루 넘게 대기하던
  `20260902-16`을 `review_cancel`로 취소하고(⚠️ **취소한 번들은 영구히 재제출 불가** — `-19`가 `-16`의 커밋을
  전부 포함하므로 잃는 것은 없다), 상세 설명을 **499자**로 다시 써 기름종이 알림을 5번 항목 + 개인정보 문장으로
  넣었다(다른 문장 약 50자 축약). 곧바로 `-19`를 `bundle_submit_review` → **REVIEWING**. 릴리스 노트는 세 묶음
  (가끔 쓰는 제품 · 화면 정리 · 기름종이 알림)으로 `-16`의 몫까지 담았다.
  ⚠️ **T-014 3번째 패턴**: 이번 `miniapp_update_basic_info`는 `reviewState`가 `APPROVED`에서 **안 움직였는데도**
  `miniapp_get`에 새 문구가 **즉시** 실렸다(검토 없이 반영). 추기의 「APPROVED 그대로면 no-op」 규칙이 깨졌다 —
  **두 신호를 다 봐야 한다**(T-014 추기 2)
- [x] ✅ **검수 승인 → 라이브**(2026-09-03 13:40:36 · `deployed: true` 실측) — 12:52 제출에서 한 시간이 안 걸렸다.
  `-16`이 21시간을 대기하던 것과 대비된다(지연 원인은 여전히 미확정 — 앱정보 정합·번들 내용 중 무엇이 갈랐는지
  가릴 근거가 없다). **v4-2 계단 1 · UX 1차 · UX 2차 · v5가 이 한 번들로 함께 출시됐다**(검수 무반려 8연속)

## ⚠️ 리스크 · 미검증 가정

- ~~플래시 500ms·6fps 체감~~ — v1 실기기 확정(상수 무조정). ~~`appName` 가용성~~ — facefit 확보. (v1 설계 §11 잔여 전부 해소)
- 타임랩스 365장 blob URL 메모리 — 저사양 기기에서 덜컹거리면 「현재±N장만 유지」로 좁힌다(화면 내부 최적화). 아직 잠재.
- v2-1 잔여: 아침 08:00 실수신(태스크 6 — templateCode 연결의 최종 검증) 대기.
- v2-2·v2-3 잔여 리스크는 각 설계 문서 §7이 단일 출처(쿼터 공유 · EE_DOC_DATA 고시 문구 패턴 의존 · 광역 앵커×하우스 브랜드 사각 등 — 전부 무음 폴백으로 피해 상한 잠김).
- **v3 리스크는 [서버 설계 §8](docs/2026-08-30-server-design.md)이 단일 출처.** 큰 것 셋:
  ① **익명 키의 기기 간 동일성** — 복원 축 전체가 얹힌 미검증 가정(v3-2가 검증 계단, 실패 시 복원 코드 폴백)
  ② **기기를 바꾸면 사진은 여전히 사라진다** — 의도된 한계, 복원 화면이 복원 **전에** 고지
  ③ **동거로 BookTimer와 장애 반경 공유** — facefit 누수는 `mem_limit`이 막지만, BookTimer 쪽 사고·재부팅은 facefit이 통째로 맞는다(방어 없음 · 수용).

## ⏸ v2 이후 (설계 §7 — 지금 안 하는 이유가 그 문서에 있다)

제품 추천·AI · 성분표 사진 · 얼굴 자동 정렬 · 두 날짜 비교 화면 · 아침/저녁 2슬롯 ·
동영상 내보내기 · 플래시 끄기 토글 ·
**사진 앨범 내보내기**(`Device.saveBase64Data` — 기기 이전 시 사진이 사라지는 것의 보완책.
서버와 무관한 클라 전용 계단이라 v3와 병행 가능 — 서버 설계 §3-2·§7)
(촬영 리마인드 푸시는 v2-1로, 제품 검색은 v2-2로, **클라우드 백업은 v3로 승격** — 위 절)

- 성분 데이터 재료는 조사 완료(2026-08-29) —
  [화장품 제품 데이터 API 리서치](docs/2026-08-29-cosmetics-data-api-research.md).
  보고품목 API는 v2-2가 직호출로 채택(프록시 전제는 실측으로 뒤집힘 — 설계 §3-1),
  성분 사전(원료성분정보)은 성분표 OCR 계단의 재료로 보류, 코리안넷은 건당 유료라 후순위.
